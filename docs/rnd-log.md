# R&D edge log — commandcenter

Lap-selection aid, not a backlog. One edge ends a lap. A clean sample is a result.

| Lap (UTC) | Probe | Edge found | Evidence | Re-run | Not sampled / suspected |
| --- | --- | --- | --- | --- | --- |
| 2026-09-18T01:29Z (cc-04e8b85c, lap 1) | Where does the control plane stop a prompt the AM4 dense rung (llama-server, 4096-token slot) cannot hold; which layer, loud or silent | **The validator does not see the pack.** A 5,336-token pack with the envelope claiming `max_context_tokens=4096` validated clean (it prices the envelope's claim against the rung's `context_bytes`, never the bytes it will send); llama-server refused it loudly through the facade — HTTP 400 `exceed_context_size_error` (n_prompt_tokens 5336, n_ctx 4096), no silent truncation; the attempt receipt kept only "HTTP Error 400: Bad Request" and dropped the body that says why | `runs/operator/run-rnd1-over6k-20260918T012916Z` (attempt_ef7e37ea…, `outcome.final` failed, 0.203 s); 400 body via direct curl to `192.168.12.233:8090/v1/chat/completions` | `bash <scratchpad>/rnd_lap1.sh over6k 4096 hearth/operator/proposal.py` from the operator-program worktree with `HEARTH_API_KEY` + `HEARTH_ROOT` set | a 3.5k–4k pack (does `max_tokens=1024` output push a near-full prompt over `n_ctx`?); the same probe through the door (pinned `am4-dense` should refuse at 14,336 bytes before dispatch — untested); drafter counting the pack's bytes itself; ingesting the 400 body into the receipt (suspected 3 lines in `execute.py`, deferred) |
| 2026-09-18T01:34Z (cc-04e8b85c, lap 2) | Derek's pivot: one NVIDIA card alone as a long-context READER (NVIDIA prefill beats the B70s); can the RTX 4070 Ti serve qwen2.5-14b at 16k through the AM4 facade + control plane, taking the 5k pack the 4k dense slot refused? | **Yes — clean sample, thesis holds.** `am4-read-4070ti` (14B Q4, 16k ctx, q8_0 KV, 10,161 MiB on the 4070 Ti alone): the 5,101-token pack validated, executed in **3.062 s** end-to-end, 5/5 checks, reconstructable; server prefill **2,849 tok/s** (5,096 tok in 1.79 s), decode 39 tok/s. Split-27B stopped (`dense.hold` set; `sudo -n` works over ssh); `am4-dense` now reports ready=False loudly. `execute.py` takes endpoint/bearer/model from the pool by rung name; scheduler probes every am4 facade rung | `runs/operator/run-rnd2-reader6k-20260918T013436Z` (attempt_31624be9…, artifact ee688d08…); `am4:~/work/am4-dual-nvidia-poc/logs/reader-4070ti.log` | `bash <scratchpad>/rnd_lap2.sh reader6k 16384 hearth/operator/proposal.py` (reader must be up on am4 :18085) | RTX 5070 as a second independent reader (next lap); a same-shape 5k pack on omen-arc for the B70 prefill number (G4's 34.9 s was a 10k pack — not comparable); the 16k ceiling; concurrent reads on both cards; a drafter rule sending `read_vs_reasoning=read` tasks to reader rungs (default still direct_hearth); the reader is a nohup, not a unit |
| 2026-09-18T01:52Z (cc-04e8b85c, lap 3, MemSplice `C:\work\memsplice` @ 730f4bc) | Run the MemSplice experiment for real: prefill on AM4's RTX 4070 Ti, ship the KV over the 1 GbE direct link with the MemSplice wire header, restore into the same model on one B70 (`qwen14b-vk1`), decode there; time every hop against the B70 prefilling itself. (The PoC's `LD_PRELOAD` capture cannot work on CUDA — device pointers, wrong symbol — so llama-server slot save/restore is the capture.) | **Restore REFUSED on the slot-state version**: `state_seq_load_file unknown (magic, version) 67677371/00000003` — AM4 `81aeaeb` writes v3, OMEN knee fork `e85caa8` reads v2. Everything before it ran with a real 989,019,916-byte KV (5,030 tok, 196,624 B/tok f16): AM4 prefill **1,728 ms (2,912 tok/s)**, save 261 ms, wire **8,405 ms at 112 MB/s**, sha equal both ends; B70 cold baseline **6,492 ms (775 tok/s)**, decode 20.6 tok/s. **Verdict for this pair: f16 KV over 1 GbE costs 1.67 ms/tok to ship vs 1.29 ms/tok B70 prefill — the wire alone loses**; break-even needs KV ≤ ~157 KB/tok (q8_0 marginal, q4_0 ≈ 1.5× win) or a 2.5/10 GbE link | `memsplice/results/{stage1.json, b70-cold-baseline.json, disaggregated-kv-20260918T015435Z.md}`; window `rot-memsplice-20260918-A` (closed `failed`, reason recorded); the version line in `hearth/var/swap-logs/qwen14b-vk1.log` | `cd C:\work\memsplice && python tests\benchmarks\bench_disaggregated_kv.py stage1` → door `rotation_window open` / `rotation_load qwen14b-vk1` / `restore_step.py` under `with-gateway-env.cmd` / `stage2` | stage 2 (decode from the shipped KV, `decode_identical`) — blocked on the version; same llama.cpp commit both ends (CUDA build of the knee fork on AM4, or rebase); q8_0/q4_0 KV to shrink the file; the 5070 as a second prefill card; a 10k+ prompt where AM4's prefill lead grows |
| 2026-09-18T01:47–01:49Z (cc-04e8b85c) — **can't answer why**, not a lap | Production `omen-arc` rate check before a rotation window (preflight G2) | Production decodes at **22–32 tok/s** (3 fresh samples: 30.7 / 22.9 / 31.5 over 32 / 32 / 692 tokens) against the 105.3 baseline; the same server instance (epoch ~23:20Z 09-17) did 90.2 on its first decode at 23:40Z and drifted 54 → 48 → 46 → 17 → 52 → 19 → 24 → 11 within the epoch; load report clean (49/49 layers on Vulkan0/1, 167 MiB host); a 692-token burst did not restore it; fx99 keep-alive timers had been stopped since 09-13 (nightshift morning step never ran) — restarted 01:46Z; no other intervention (INC-2026-08-30-A: watch, do not poke) | `hearth/var/arc-serve.log` `print_timing` rows; `hearth/var/arc-keepalive.jsonl` tail | `ssh fx99 sudo systemctl start arc-keepalive-deep.service` then `query_rung_state` | whether `ArcServeBoot` restores 105; thermal/clock; WDDM paging (ADR-0043) vs a real regression; whether the drift correlates with the G3/G4 runs' deep-context slots |
| 2026-09-18T02:12Z (cc-04e8b85c, lap 4a) | Derek's edge: the RTX 5070 (Blackwell, GDDR7 672 GB/s) is the newer card — is it the faster prefill seat? Second 14B reader on the 5070 (:18086), same flags as the Ti reader, same 5,030-token prompt, two rounds each, slot erased before every run | **No — the 4070 Ti wins prefill by 17%**: 5070 **2,493 / 2,500 tok/s**, 4070 Ti **2,920 / 2,929 tok/s** (2,017 vs 1,720 ms). Build carries `89-real;120a-real` (native Blackwell kernels present), so it is physics: prefill is compute-bound and the Ti has 60 SMs / ~40 TFLOPS vs the 5070's 48 SMs / ~31; GDDR7 bandwidth buys decode, not prefill. **Ti = prefill seat; 5070 = second reader.** Both readers resident (10.1 GB each) | `am4:~/work/am4-dual-nvidia-poc/logs/reader-5070-f16.log`, `reader-4070ti-f16.log` (print_timing rows) | `scp results/stage1-body.json am4:/tmp/memsplice-body.json; ssh am4 'for port in 18086 18085; do curl -s -X POST http://127.0.0.1:$port/slots/0?action=erase -d {}; curl -s http://127.0.0.1:$port/completion -d @/tmp/memsplice-body.json; done'` | 5070 decode rate (should beat the Ti on bandwidth — unmeasured); both cards prefilling two prompts concurrently (the prefill-farm shape); the 30B-A3B split with `--main-gpu` on the Ti instead of the 5070 (plan said 5070 — revise) |
| 2026-09-18T02:17Z (cc-04e8b85c, lap 4, MemSplice) | Same llama.cpp commit on both ends (`e85caa8`: OMEN's knee Vulkan binary; CUDA build of it on AM4, `-DCUDAToolkit_ROOT` + `-L/-rpath-link` on the toolchain lib dir was the missing link flag), then stage 2: restore the CUDA-made KV into the B70 and decode | **Cross-vendor KV hand-off is CORRECT**: restore 5,030 tokens / 989 MB in **309 ms**, replay `prompt_n=1 cache_n=5029`; B70 decode from the shipped cache **identical to its own cold decode for 64 tokens at T=0**. Speed: disaggregated **11,562 ms** (1,734 prefill + 1,114 save + 8,406 wire + 309 restore) vs B70 local prefill **6,542 ms** → **0.57×**, wire = 73% of it — the physics said so; the correctness proof is the result | `memsplice/results/disaggregated-kv-20260918T021803Z.{json,md}`, `results/restore-receipt.json`, `results/stage1.json`; window `rot-memsplice-20260918-B` (closed `passed`); memsplice README result section | `cd C:\work\memsplice && python tests\benchmarks\bench_disaggregated_kv.py stage1` → `rotation_window open` / `rotation_load qwen14b-vk1` / `restore_step.py` (with-gateway-env) / `stage2 --restore-receipt results\restore-receipt.json` | lap 5: 30B-A3B (4 KV heads) with q8_0 KV on both ends at 16k/32k — the projected win; q8_0 restore across vendors (untested: quantized cache types must match and the Vulkan q8 KV path is the yaml's own open note); the streaming detour on `llama_state_seq_save_file`; save_ms varied 261 → 1,114 ms between runs (disk cache) — the wire, not the save, is the cost |
| 2026-09-18T02:23Z (cc-04e8b85c, lap 5, MemSplice) | The use case with the odds: Qwen3-30B-A3B (4 KV heads), q8_0 KV on both ends, 16k and 30k prompts; prefill on AM4's two NVIDIA cards (layer split, CUDA e85caa8), wire, restore into ONE B70 (standalone Vulkan seat, same commit, production held down under `arc-maintenance.stop`), decode; vs the B70 prefilling itself | **WIN — 3.07× at 16k, 5.29× at 30k.** 16k: AM4 prefill 2370 ms (6674 tok/s) + save 228 + wire 7.0234 s (112.21 MB/s, 52,240 B/tok) + restore 427 ms = **10048 ms** vs B70 local **30836 ms** (513 tok/s). 30k: **19556 ms** vs **103508 ms** (288 tok/s — the knee: 3.5 ms/tok). Hot decode from the shipped q8_0 cache is coherent and gives the same answer but is NOT byte-identical (first-token phrasing differs) — CUDA vs Vulkan q8_0 rounding; f16 was identical in lap 4. Also: `ArcServeRestart` kills every `llama-server.exe`, and without the sentinel the watchdog re-boots production within minutes — the nightshift runbook step, relearned | `memsplice/results/moe-longctx-20260918T022840Z.{json,md}`; `hearth/var/swap-logs/memsplice-moe-8095.log`; `am4:~/work/am4-dual-nvidia-poc/logs/moe-dual-q8kv.log` | seats: AM4 `llama-server --model Qwen3-30B-A3B... -ts 1,1 -c 32768 -ctk q8_0 -ctv q8_0 --port 18090 --slot-save-path kvslots/` (e85caa8 build); OMEN `llama-server.exe -m ...Qwen3-30B-A3B... -c 32768 -np 1 -ctk q8_0 -ctv q8_0 --port 8095 --slot-save-path E:\work\battlemage\kv\` with `GGML_VK_VISIBLE_DEVICES=1`, sentinel set; then `python tests\benchmarks\bench_moe_longctx.py --tokens 16000 --tokens 30000` | streaming the KV during prefill (detour on `llama_state_seq_save_file`) — would cut the AM4-side 2.6–5.9 s; dual-B70 local prefill (production shape, `-ts 1,1`) as the fairer baseline — the 1,354 tok/s at 10k this epoch says the split helps but not 5×; whether q8_0 divergence grows with decode length; q4_0 KV (half again on the wire); the 5070 alone for the MoE (3B active might fit one 12 GB card at short ctx) |
| 2026-09-18T02:29Z (cc-04e8b85c) — follow-up to the can't-answer-why row | Does a fresh `ArcServeBoot` restore production's decode rate? | **Yes: 97.3 tok/s** (692 tokens in 7.11 s) right after the boot, vs 22–32 tok/s before it. The evening's degradation is within-epoch drift that a restart clears, not a regression; the cause of the drift is still unexplained | `hearth/var/arc-serve.log` (new epoch), the door result for job_7896f979 | `schtasks /Run /TN ArcServeBoot` then one 700-token pinned `omen-arc` call | what drifts (WDDM paging vs fragmentation vs thermal); how fast it drifts on a quiet box (the keep-alive deep timer is back on fx99, so the tail will show it) |
| 2026-09-18T02:35Z (cc-04e8b85c, lap 6, MemSplice) | The fair baseline: same lap-5 KV files, but the OMEN seat is the 30B-A3B across BOTH B70s (`-ts 1,1`, q8_0, 32k — the production shape); it restores + decodes, and prefills the same prompts itself | **Win survives: 1.806× at 16k, 2.928× at 30k.** Dual-B70 prefill 887 / 520 tok/s (single card was 513 / 288); restore 248 / 431 ms; decode at depth unchanged (19.0 / 10.8 tok/s). Disaggregated 9869 / 19571 ms vs local 17826 / 57310 ms; the wire is ~70% of the disaggregated side. Production booted back `at_rate` (110 tok/s) afterwards | `memsplice/results/moe-longctx-dualb70-*.{json,md}`; `hearth/var/swap-logs/memsplice-moe-dual-8095.log` | sentinel + `ArcServeRestart`; dual seat `llama-server.exe -m ...30B-A3B... -sm layer -ts 1,1 -c 32768 -np 1 -ctk q8_0 -ctv q8_0 --port 8095 --slot-save-path E:\work\battlemage\kv\`; `python tests\benchmarks\bench_moe_longctx.py --tokens 16000 --tokens 30000 --omen-only-from results\moe-longctx-20260918T022840Z.json --label dualb70`; then remove sentinel + `ArcServeBoot` | streaming detour; q4_0 KV; the AM4 prefill seat as a control-plane route; the production `-np 8` shape (16k slots) cannot even take the 30k prompt — the disaggregated path handles a prompt production refuses |
| 2026-09-19T00:33–00:52Z (cc-544e4480, lap 7, MemSplice) | Qwen3.8-27B — a DENSE model (all 27B weights per token) with hybrid attention (qwen35 arch: 48 linear/recurrent + 16 full-attention layers, 4 KV heads × 256) — prefilled on AM4's 4070 Ti + 5070 (layer split, CUDA `e85caa8`, q8_0 KV, 32k slot), slot-saved, shipped over the 1 GbE P2P link, restored into the same model on OMEN's B70s. Does the dense win where the 14B dense lost, and how does it compare against ONE B70 and against BOTH B70s (`-sm layer -ts 1,1`, the production shape)? | **Two edges, one lap. (1) The first pass reported 3.14×/4.86× but the restored cache was silently DISCARDED** — `n_restored=15828` yet the hot request showed `cache_n=0, prompt_n=15828` and took the full 42.5 s. Cause, read from `tools/server/server-context.cpp:3223-3314` (PR #24110): with no new tokens `pos_min_thold = pos_next - 1`, the recurrent memory's `pos_min` is the last position, the server searches for a context checkpoint (none in a freshly restored slot) and forces `n_past = 0` — a hybrid/recurrent model cannot rewind its state one token to evaluate ≥1. **Fix: ≥1 new token after the restored prefix** (`--hot-suffix " The"`) → `cache_n=15828, prompt_n=1`. That is the real shape anyway (doc, then question). **(2) The win is real for the dense 27B, and larger than the MoE's: vs ONE B70 3.14× at 16k / 4.90× at 30k; vs BOTH B70s 1.92× / 2.74×.** AM4 pair prefills the 27B at 2239 / 2197 tok/s; one B70 370 / 247; both B70s 609 / 443. KV is 44,752 / 40,094 B/token q8_0 (LIGHTER than the 30B-A3B's 52,240 — only 16 of 64 layers carry KV; the recurrent state is a fixed ~150 MiB `RS buffer`, not per-token). Disaggregated 13.5 s / 24.6 s vs single 42.8 / 121.0 s vs dual 26.0 / 67.5 s. Decode from the shipped cache 15.2 / 11.4 tok/s (dual), same as local. Wire is 45 % / 41 % of the disaggregated side (less than the MoE's 70 % — the 27B's prefill on AM4 is the larger term now). Decode equivalence is THIN: hot and cold agree on the tokens both produced (`' entry point is'`) but this raw `/completion` prompt makes the chat-tuned 27B emit EOS at the margin (cold at 16k stopped after 4 tokens, hot went on into a `<think>` block; at 30k the reverse) — a q8_0-margin EOS decision, not a broken recurrent state, but 4 tokens is not 48. Also: the first single-token step on a fresh seat cost 5.4 s (`prompt_ms` for 1 token at 16k on the single seat; 164 ms at 30k after warm-up) — a one-time shader/pipeline warm-up, excluded from the totals above | `memsplice/results/moe-longctx-27b-singleb70-20260919T004305Z.*` (the discarded-cache pass), `...-27b-singleb70-suffix-*.{json,md}`, `...-27b-dualb70-*.{json,md}`; `hearth/var/swap-logs/memsplice-27b-single-8095.log`, `memsplice-27b-dual-8095.log`; `am4:~/work/am4-dual-nvidia-poc/logs/dense27b-dual-q8kv.log`; door job_976282d4 (production at rate after `ArcServeBoot`) | AM4: kill the `:18090` seat, `LD_LIBRARY_PATH=$PWD/toolchain/cuda-13.2/lib:$PWD/llama.cpp-knee/build/bin ./llama.cpp-knee/build/bin/llama-server --model models/Qwen3.8-27B-Q4_K_M.gguf --host 127.0.0.1 --port 18090 --n-gpu-layers all --split-mode layer --tensor-split 1,1 --main-gpu 0 --ctx-size 32768 --parallel 1 --batch-size 2048 --ubatch-size 1024 --flash-attn on --cache-type-k q8_0 --cache-type-v q8_0 --jinja --slots --slot-save-path .../kvslots/ --log-verbosity 5`; OMEN: `ssh fx99 sudo systemctl stop arc-keepalive.timer arc-keepalive-deep.timer` FIRST (runbook step 1 — skipped this lap; the sentinel fenced the deep unit's recovery path, pings just failed for 17 min), sentinel + `ArcServeRestart`, then single `GGML_VK_VISIBLE_DEVICES=1 llama-server.exe -m ...qwen38\Qwen3.8-27B-Q4_K_M.gguf -ngl 99 -fa on --no-mmap -dio -fit off -c 32768 -np 1 -ub 1024 -ctk q8_0 -ctv q8_0 --port 8095 --slot-save-path E:\work\battlemage\kv\` (dual: drop the env var, add `-sm layer -ts 1,1`); `python tests\benchmarks\bench_moe_longctx.py --tokens 16000 --tokens 30000 --model-tag qwen38-27b-q8kv --hot-suffix " The" --label 27b-singleb70` then `--omen-only-from <that json> --label 27b-dualb70`; remove sentinel + `ArcServeBoot` + `ssh fx99 sudo systemctl start arc-keepalive.timer arc-keepalive-deep.timer`; `query_rung_state` read at_rate 109.7 tok/s (104 %) at 01:14Z | 48-token decode equivalence with `ignore_eos` (or the chat endpoint) — the recurrent-state crossing is inferred from 4 agreeing tokens; q4_0 KV (would take the wire to ~20 KB/token); the 5.4 s first single-token step is NOT ADR-0043 idle-cold (idle gap < 30 s, and fx99's `arc-keepalive.timer` keeps production warm anyway — Derek, 2026-09-19): every first-pass request reported `predicted_ms=0.001`, i.e. EOS sampled off the prefill logits, so no batch-1 forward had ever run on that seat — a Vulkan pipeline compile for the recurrent layers at batch 1, paid once per fresh seat, never on production; the AM4 4096-token slot in the `am4-dense` rung vs the 32k this lap needed (~2 GB VRAM headroom per card at 32k — 64k will not fit); Flash-Next (the MoE) untouched; streaming detour still unbuilt; AM4 seat LEFT UP on the 27B (`:18090`), so `am4-dense`/`am4-read-4070ti` remain DOWN |
| 2026-09-19T01:20–01:36Z (cc-544e4480, lap 8, MemSplice) | Reframed by Derek: the scoreboard is total throughput × accuracy of work completed on local hardware with the B70s kept decoding — not per-request latency — and the Qwen3.8 models get better over longer tasks. So: how deep a context can the AM4 prefill farm (4070 Ti + 5070, 12 GB each) produce for the dense 27B? Ladder 64k → 96k → 128k → 160k+ with q8_0 then q4_0 KV; loud refusal at load, or a real prefill at that depth. AM4-only; production untouched | **Ceiling = 128k with q4_0 KV (96k with q8_0); 160k refused.** 64k q8_0: loads, **59,950-token prefill in 31.6 s = 1898 tok/s**, KV 37.5 KB/token, 5070 at 11.4/12.2 GB, VRAM FLAT during prefill (every buffer is reserved at load — "loads" = "serves" on this build). 96k q8_0: loads at 11.7 GB. 128k q4_0: loads with 849 MiB free; **119,203-token prefill in 81.5 s = 1462 tok/s**, KV **19.8 KB/token**, slot file 2.36 GB. 160k / 192k / 256k q4_0: `cudaMalloc failed: out of memory` on device 0 for the fit-pass compute buffer (2161 / 2545 / 3313 MiB) — the worst-case reservation binds, not the KV. `--main-gpu 1` moved nothing (identical VRAM); `-ub 512` did not help. AM4's own knee is gentle: 2239 (16k) → 2197 (30k) → 1898 (60k) → 1462 (120k) tok/s. Production's 16k slots cannot take any of these; the dual B70s' measured knee (609 → 443 tok/s from 16k to 30k) puts a 120k local prefill past ten minutes — EXTRAPOLATED, not measured. The lap-7 claim "64k won't fit" was napkin math and wrong by 2× | `memsplice/results/am4-ceiling-64k-q8-20260919T013110Z.json`, `am4-ceiling-128k-q4-20260919T013526Z.json`; `am4:~/work/am4-dual-nvidia-poc/logs/ceiling-c*.log` (one per rung, the refusals included), `logs/dense27b-dual-q8kv-64k.log`; slot files `am4:.../kvslots/qwen38-27b-q8kv.0.6fd63cf35a0120e8.bin` (2.25 GB, 60k) and `qwen38-27b-q4kv.0.705bb11bc87f92a5.bin` (2.36 GB, 119k) | `scp memsplice/am4/tools/ceiling_ladder.sh am4:/tmp/ && ssh am4 /tmp/ceiling_ladder.sh 65536,q8_0,0,1024 98304,q8_0,0,1024 131072,q4_0,0,1024 163840,q4_0,0,1024` (args: ctx,kv-type,main-gpu,ubatch; leaves the last loaded rung on :18090); then `python tests\benchmarks\bench_am4_ceiling.py --tokens 120000 --label 128k-q4 --model-tag qwen38-27b-q4kv` (prompt from `operator-program\hearth\**\*.py`, tokenized by the AM4 seat) | the OMEN side at these depths (a q4_0 KV file into a q4_0 B70 seat — KV TYPE must match as well as commit, untested); the two-stage split prefill (AM4 does what fits, the B70 continues from n_past — the `--hot-suffix` mechanism with a 16k+ suffix); `-b 1024` to shrink the fit-pass reservation for 160k; a smaller weight quant (IQ4_XS ≈ −4 GB); decode quality at 120k from a q4_0 cache (accuracy is half of Derek's scoreboard and nothing here measured it); the pkill self-match trap bit twice (`pkill -f` matches the ssh shell's own argv — use a pid file); AM4 LEFT on the 128k q4_0 seat (`:18090`) |
| 2026-09-19T01:40–02:00Z (cc-544e4480, lap 9, MemSplice) | The OMEN side at depth: ship the 119,203-token **q4_0** cache (Qwen3.8-27B, made on AM4's CUDA pair in lap 8) over the wire, restore it into a q4_0 dual-B70 seat at 128k, decode 96 tokens; then have the dual B70s prefill the same 119k themselves — the number lap 8 only extrapolated; read both answers for accuracy (the other half of Derek's scoreboard) | **Three clean samples, one edge. (1) q4_0 KV crosses CUDA → Vulkan and is USED: `cached n_tokens = 119203`, restore 873 ms, only the 7 suffix tokens evaluated (1.4 s).** (2) **The dual B70s' own 119k prefill: 1,014.8 s = 16.9 min at 117 tok/s** (the knee: 927 tok/s at 4k → 164 cumulative at 94k → ~75 instantaneous) — vs AM4 81.5 s + wire 20.0 s (2.36 GB at 112 MB/s) + restore 0.9 s = **~103 s, 9.8×**. On the throughput scoreboard: 16.9 min of B70 silence becomes 1.7 min per 119k task. (3) **Accuracy at 119k through the shipped q4_0 cache is exact**: the hot answer names `hearth/backlog/__main__.py` and its `list` / `promote-refine` / `validate` subcommands dispatching to `_cmd_*` handlers — verified against the source; the cold (local-prefill) answer reaches the same file through a longer `<think>` enumeration. Not byte-identical (10 common chars; the think-vs-answer decision is the q4_0 margin, as in laps 5/7) but the same substance. **Decode at 119k depth on both B70s: 4.9 tok/s** (202 ms/token) hot and cold alike — the dense 27B at depth is slow whichever way its cache arrived; that is the cost of the ceiling, and the reason the B70s should never also be the ones prefilling. Runbook done right this lap: fx99 timers stopped before the sentinel, restarted after `ArcServeBoot`; door round-trip 250 ms afterwards | `memsplice/results/omen-depth-119k-q4-dualb70-20260919T015951Z.json` (both generations in full); `hearth/var/swap-logs/memsplice-27b-dual-128k-q4-8095.log` (task 2 = hot, task 101 = cold with per-2048 progress lines); wire receipts in the transcript (sha `f0070070…` both ends); door job_acbae489 | AM4 seat at 128k q4_0 up (lap 8); `ssh fx99 sudo systemctl stop arc-keepalive.timer arc-keepalive-deep.timer`; sentinel + `ArcServeRestart`; `llama-server.exe -m ...Qwen3.8-27B-Q4_K_M.gguf -ngl 99 -sm layer -ts 1,1 -fa on --no-mmap -dio -fit off -c 131072 -np 1 -ub 1024 -ctk q4_0 -ctv q4_0 --port 8095 --slot-save-path E:\work\battlemage\kv\`; wire: `python omen\kv_receiver\omen_kv_receiver.py --bind 10.44.0.1 --port 9999 --out E:\work\battlemage\kv\<file>` + `ssh am4 python3 /tmp/send_kv_file.py --file .../kvslots/<file> --name kv_state.qwen38-27b-q4kv --host 10.44.0.1 --port 9999`; `python tests\benchmarks\bench_omen_depth.py --body results\ceiling-body-705bb11bc87f92a5.json --filename qwen38-27b-q4kv.0.705bb11bc87f92a5.bin --n-predict 96 --ignore-eos --label 119k-q4-dualb70`; kill seat, remove sentinel, `ArcServeBoot`, fx99 timers `start` | one accuracy sample is not a regime (one question, one prompt; a task suite at depth is the real measure); decode at 4.9 tok/s at 119k — where the B70s' time goes now, and whether `-np 2+` at depth adds throughput (two long contexts decoding together) or just halves each; the two-stage split prefill (AM4 128k + B70 continuing) untested; streaming detour still unbuilt (wire 20 s could hide under AM4's 81 s); q8_0 vs q4_0 accuracy at depth; the driver's quoting put the received file at `kv$F` once (moved by hand — fix the wire step in the driver); AM4 LEFT on the 128k q4_0 seat |
| 2026-09-19T02:13–02:45Z (cc-544e4480, lap 10, MemSplice) | CAPACITY (Derek: limits, not speed): how many 119k q4_0 contexts of the dense 27B can the dual B70s hold, and does decoding 1 / 2 / 4 / 8 of them together raise aggregate tok/s above the 4.9 single-stream figure? Seat `-np 8 -c 1048576` (128k per slot), the lap-8 cache restored into each slot, N concurrent completions pinned by `id_slot` | **Three seat rules learned by refusal, then a capacity edge. (a) `n_stream mismatch`**: a slot file saved from a `-np 1` seat carries one KV stream and a non-unified `-np 8` seat refuses it — the consumer must run **unified KV (`-kvu`)**, or match the producer's `-np`. **(b) `--cache-ram 0`**: the default 8 GiB host prompt cache "saves idle slots to prompt cache and clears them upon starting a new task" — slot 1's restored 119k was evicted the instant slot 0's request began, and a by-id request found the slot empty and re-prefilled 119k from zero. **(c) Fresh restore before every request** on a recurrent-layer model — after a round the slot holds prompt+suffix+generated tokens, the next identical prompt is a strict PREFIX of the cache, the server must evaluate ≥1 token, cannot rewind, `n_past=0` (lap 7's edge, second guise). With all three: **eight 119k contexts load and restore (1M tokens resident, ~20 of 32 GB per card)** and all eight decode from their caches — but **per-slot decode is inversely proportional to TOTAL RESIDENT CELLS, not to concurrency**: 1 slot/119k cells 4.88 tok/s; 2/238k 1.5 each; 4/476k 0.74 each; 8/953k 0.61 each → aggregate FLAT at ~3–5 tok/s. On this Vulkan FA path a unified pool makes every sequence attend over every resident cell — the eight-context capacity is real for memory and fake for compute. Confirmed by lap 11's discriminating run: the same single sequence decoded at 0.75 tok/s with 953k cells resident and 4.47 tok/s on a non-unified seat holding only itself | `memsplice/results/np-depth-119k-q4-dualb70-kvu-noram-20260919T024218Z.json`; seat logs `hearth/var/swap-logs/memsplice-27b-dual-np8-1m-q4-{8095,kvu-8095,kvu-noram-8095}.log` (the n_stream refusal, the prompt-cache eviction lines `saving idle slot to prompt cache` / `skipping, slot is empty`, and the clean ladder respectively) | fx99 timers stop; sentinel + `ArcServeRestart`; `llama-server.exe -m ...Qwen3.8-27B-Q4_K_M.gguf -ngl 99 -sm layer -ts 1,1 -fa on --no-mmap -dio -fit off -c 1048576 -np 8 -kvu --cache-ram 0 -ub 1024 -ctk q4_0 -ctv q4_0 --port 8095 --slot-save-path E:\work\battlemage\kv\`; `python -u tests\benchmarks\bench_np_depth.py --body results\ceiling-body-705bb11bc87f92a5.json --filename qwen38-27b-q4kv.0.705bb11bc87f92a5.bin --concurrency 1 2 4 8 --n-predict 64` | whether a NON-unified `-np 8` seat (per-stream cells, per-sequence attention) batches deep decodes with real gain — blocked by the n_stream check in `llama_state_seq_load_file`; a patch letting a 1-stream file land in stream `dest_seq_id` of an n-stream cache is the multiplier (knee-campaign lane); the same ladder at 16k depth (does batching pay where attention is cheap?); whether the flat aggregate is the FA kernel or the server's batch construction; AM4 LEFT on the 128k q4_0 seat |
| 2026-09-19T02:45–03:01Z (cc-544e4480, lap 11, MemSplice) | CAPABILITY: a prompt PAST the farm's 128k ceiling — restore AM4's 119k cache on the B70s, then send a 133k prompt (the cached prefix + 13.8k tokens of further source + the question) so the B70s prefill only the tail from `n_past = 119203`. Does it serve, at what tail rate, and is the answer still right? | **Serves, correctly, and the split is the only way this prompt gets served at all** (AM4's ceiling is 128k; production's slots are 16k). `cache_n=119203, prompt_n=13828`. On the lap-10 unified 1M seat with seven other 119k caches resident: tail 581 s (24 tok/s), decode **0.75 tok/s** — the pool tax. On a non-unified `-np 1 -c 163840` seat holding only this sequence: **tail 13,828 tokens in 313.6 s = 44 tok/s, decode 4.47 tok/s at 133k**, identical text. So depth alone costs 4.9 → 4.47 on decode; the unified pool cost 1.85× on tail prefill and 6× on decode. The answer got richer with the extra files: `hearth/backlog/__main__.py`, dispatching to `promote-refine` (→ `sources_mod.promote_refine`), `list` (→ `select_mod.select_next` over the three source scans) and `validate` — verified against the source in lap 9. Time to first token for a 133k task via split: AM4 81.5 + wire 20 + restore 1.2 + tail 314 ≈ **417 s**, vs B70-only ≥ 1,015 + 314 ≈ 1,330 s (the 119k part measured in lap 9, the tail measured here) ≈ 3.2× — the win is exactly the fraction the farm carried | `memsplice/results/split-prefill-135k-q4-dualb70-20260919T025425Z.json` (unified pool) and `split-prefill-135k-q4-dualb70-np1-20260919T030049Z.json` (single slot); `hearth/var/swap-logs/memsplice-27b-dual-np1-160k-q4-8095.log`; door job_a4ee7106 (production back, 234 ms) | seat `... -c 163840 -np 1 --cache-ram 0 -ub 1024 -ctk q4_0 -ctv q4_0 --port 8095 ...`; `python -u tests\benchmarks\bench_split_prefill.py --body results\ceiling-body-705bb11bc87f92a5.json --filename qwen38-27b-q4kv.0.705bb11bc87f92a5.bin --tail-tokens 16000 --n-predict 64 --label 135k-q4-dualb70-np1` (tail = the next files of the same sorted glob, tokenized by the OMEN seat); then kill seat, remove sentinel, `ArcServeBoot`, fx99 timers start | the B70 tail rate past 133k (44 tok/s at 120–133k; the curve beyond is unsampled — 256k is the model's train ctx and the B70s have the VRAM); a tail that is a real task continuation (tool output, a diff) rather than more files; iterating the split — ship the B70's grown cache BACK to AM4? (wire is symmetric; AM4 cannot hold 133k, so no); q8_0 vs q4_0 answer quality at 133k; whether `-ub 2048` helps the tail on the B70s |
| 2026-09-19T03:05–03:50Z (cc-544e4480, lap 12, MemSplice → llama.cpp) | Derek: open to a second upstream PR (first: ggml-org #27652). Patch `llama_kv_cache::state_read` so a single-sequence slot state written by a 1-stream (`-np 1` / unified) cache restores into a slot of a NON-unified n-stream cache; then the lap-10 ladder with per-sequence attention. Does per-sequence attention make eight 119k contexts decode with real aggregate gain? | **Patch works; the multiplier is smaller than hoped and different in kind.** Restore across layouts: `n_stream mismatch (state 1, cache 8) - restoring seq 3 across layouts` → `n_restored=119203` in 1.19 s into a non-unified `-np 8` seat; all 8 slots served from their caches. Ladder (per-slot / aggregate): 1 → 4.90 / 4.9; 2 → 2.79 / 5.6; 4 → 1.46 / 5.9; 8 → 0.75 / 6.0. **Aggregate +20 % at best — at 119k the decode step is attention-COMPUTE-bound (~200 ms of GPU per sequence per token) and N sequences cost N×; attention work is not shared the way weight reads are, so batching does not amortize it.** ~6 tok/s is this pair's decode ceiling at this depth whatever the layout. **What the patch DOES buy: idle residency is free.** One sequence decoding with seven idle 119k contexts resident: **4.89 tok/s non-unified vs 0.75 unified** (lap 11) — the 6.5× tax on holding contexts is gone; the B70s can be a parking lot for eight long tasks and serve any one of them at full single-stream rate. Also: the fresh build tree needed `vulkan-1.dll` copied beside the exe (two modal `System Error` dialogs, killed); production runs from `build/` so it must never be rebuilt in place (`LNK1104` on the locked `ggml-base.dll` was the loud refusal). Upstream master has since refactored the function into `state_read_sinfo` with mirrored `sinfos` vectors; the PR is rebased there (index sinfos by destination stream when layouts differ), compile-checked on master, NOT re-measured on master (the slot-state format moved to v3 — re-saving from a master-built AM4 is the reproduction path) | `memsplice/results/np-depth-119k-q4-dualb70-nonunified-patched-20260919T032857Z.json`, `...-1of8resident-20260919T032942Z.json`; `hearth/var/swap-logs/memsplice-27b-dual-np8-1m-q4-nonunified-patched-8095.log` (the across-layouts warning at 0.11.527); fork commit `E:\work\llamacpp-knee` branch-local (measured build: `build-nstream/`, `build 51, commit 242c3cd` + patch); PR worktree `E:\work\llamacpp-knee-pr` branch `kv-state-restore-across-stream-layouts` at `0586db7` on `origin/master` (60081bb), body in `PR-BODY-kv-state-restore-across-stream-layouts.md` | build: `scratchpad\build-nstream.cmd` (vcvars64 → cmake -S . -B build-nstream -G Ninja -DGGML_VULKAN=ON … → --target llama-server; copy `build/bin/vulkan-1.dll` beside the exe); seat: `build-nstream\bin\llama-server.exe -m …Qwen3.8-27B-Q4_K_M.gguf -ngl 99 -sm layer -ts 1,1 -fa on --no-mmap -dio -fit off -c 1048576 -np 8 --cache-ram 0 -ub 1024 -ctk q4_0 -ctv q4_0 --port 8095 --slot-save-path E:\work\battlemage\kv\` (NO `-kvu`); `python -u tests\benchmarks\bench_np_depth.py --body results\ceiling-body-705bb11bc87f92a5.json --filename qwen38-27b-q4kv.0.705bb11bc87f92a5.bin --concurrency 1 2 4 8 --n-predict 64`; push: `cd E:\work\llamacpp-knee-pr && git push fork kv-state-restore-across-stream-layouts` (Derek's action) | the mirrored-cache (`sinfos_in/out`, iSWA pair) path across layouts — untested, named in the PR body as the part for a reviewer's eye; the PR measured on master (needs a v3 state from a master-built AM4); `-sm row` on Vulkan (layer split runs the two cards in SERIES per token at depth — a row split would put both on every layer's attention; Vulkan row-split support unknown); the same ladder at 16k depth (where batching should pay); a faster FA decode kernel over long q4_0 KV is the actual ceiling-raiser (knee-campaign territory) |
| 2026-09-19T03:55–04:20Z (cc-544e4480, lap 12b, MemSplice) | Derek: bake the across-layouts restore into AM4 first; no upstream PR today. Apply the same commit to AM4's CUDA fork of `e85caa8`, rebuild, prove it there with a state produced and consumed on the same host | **Baked and proven on AM4.** `git am` of the e85caa8-era commit → `5d3ef7cf8` on AM4's fork; incremental `cmake --build build --target llama-server -j 12` (the cache already carried `CUDAToolkit_ROOT` + the rpath-link flags) → `build 10583, commit 5d3ef7cf8`. Proof: a 29,923-token q4_0 state saved by a `-np 1` seat (2197 tok/s prefill, 23.7 KB/token) restored into **slot 1 of a non-unified `-np 2` seat** — `state_read: n_stream mismatch (state 1, cache 2) - restoring seq 1 across layouts`, `n_restored=29923` in 97 ms — and a request pinned to that slot served from it: `cache_n=29923, prompt_n=7`, correct answer. Both ends of the pipeline now run identical source (e85caa8 + the patch: OMEN `build-nstream/bin` build 51, AM4 build 10583). Two loud refusals on the way: `-np 2 -c 131072` on the 12 GB cards is a CUDA OOM (a second recurrent-state buffer + the fit-pass compute), `-c 98304` fits; and `pkill -f` matched the ssh shell's own argv for the third time — patterns are now anchored to the binary path (`^\./llama.cpp-knee/build/bin/llama-server`). Side observation: **the NVIDIA pair decodes the 27B at 30k depth at 23.7 tok/s — twice the B70 pair's 11.4 at the same depth** (lap 7) | `memsplice/results/am4-ceiling-am4-patched-np1-q4-20260919T041010Z.json`; `am4:~/work/am4-dual-nvidia-poc/logs/{nstream-build,dense27b-np1-64k-q4-patched,dense27b-np2-128k-q4-patched (the OOM),dense27b-np2-96k-q4-patched,dense27b-np1-128k-q4-patched}.log`; state `am4:.../kvslots/qwen38-27b-q4kv-am4.0.61c18f245bcaae9a.bin` (709 MB); AM4 fork commit `5d3ef7cf8` | `scp <0001-kv-cache-...patch> am4:/tmp/nstream.patch && ssh am4 'cd ~/work/am4-dual-nvidia-poc/llama.cpp-knee && git am /tmp/nstream.patch && LD_LIBRARY_PATH=$PWD/../toolchain/cuda-13.2/lib cmake --build build --target llama-server -j 12'`; producer seat `... --ctx-size 65536 --parallel 1 -ctk q4_0 -ctv q4_0 --port 18090 --slot-save-path .../kvslots/` + `python tests\benchmarks\bench_am4_ceiling.py --tokens 30000 --model-tag qwen38-27b-q4kv-am4`; consumer seat `... --ctx-size 98304 --parallel 2 --cache-ram 0 -ctk q4_0 -ctv q4_0` + `curl -X POST :18090/slots/1?action=restore -d '{"filename":"qwen38-27b-q4kv-am4.0.61c18f245bcaae9a.bin"}'`; AM4 left on `--ctx-size 131072 --parallel 1 --cache-ram 0` (patched) | OMEN production still runs the UNPATCHED `build/` (build 50); baking it there = pointing `fleet/arcserve/llama-swap/omen.yaml` at `build-nstream/bin` (or rebuilding a fresh tree from the fork branch) — takes effect at the next ArcServe restart, Derek's call; the AM4 decode-at-depth advantage (23.7 vs 11.4 tok/s at 30k) is one sample and inverts the 'B70s decode, AM4 prefills' split for the dense 27B at depth — worth a ladder of its own; the fork branches are local on both hosts (`vulkan-mmv-max-cols-env`+dangling `cdefa9d` on OMEN, `5d3ef7cf8` on AM4) — nothing pushed |
| 2026-09-19T04:25–04:45Z (cc-544e4480, lap 12c, MemSplice) | Derek: patch OMEN production too, now, so it is not forgotten. Cleanest form: the canonical fork tree carries the commit and production's own `build/` is rebuilt from it, so binary and source agree and `omen.yaml` stays untouched | **Done; production at rate on the patched binary.** `git cherry-pick cdefa9d` onto `vulkan-mmv-max-cols-env` → `60cdd25` (= 242c3cd + the across-layouts restore). Window: fx99 timers stopped → sentinel → `ArcServeRestart` (llama-swap and llama-server both gone, `:8082` down — the in-place `LNK1104` lock cleared) → `cmake --build build --target llama` relinked `llama.dll` AND `ggml-vulkan.dll` (Ninja had the ggml tree stale since the Aug 22 build) → `--target llama-server` so the binary's own version string tells the truth: **`build 52 (60cdd25)`** in `arc-serve.log`, same shape (`n_slots = 8, n_ctx_slot = 16384, kv_unified = false`). Gate: the first deep probe read 73.99 tok/s "degraded" — fired in the same second as my 492-token door call was being served (a contended sample, not a regression); the next deep probe on a quiet rung **110.64 tok/s, `decode_degraded: false`**; `query_rung_state` → **at_rate, 105 % of the epoch baseline**. Door round-trip normal. All three builds now carry the change: OMEN production `build/` (build 52, 60cdd25), OMEN `build-nstream/` (build 51), AM4 (build 10583, 5d3ef7cf8) | `hearth/var/arc-serve.log` (new epoch, build line at 0.00.149), `hearth/var/arc-keepalive.jsonl` (21:37:05 contended deep probe, 21:42:06 clean 110.64), door job_5e1a00d6, `query_rung_state` at 04:43Z; fork `E:\work\llamacpp-knee` @ 60cdd25 (local, not pushed); build logs in the session scratchpad | `cd E:\work\llamacpp-knee && git cherry-pick cdefa9d`; `ssh fx99 sudo systemctl stop arc-keepalive.timer arc-keepalive-deep.timer`; sentinel; `schtasks /Run /TN ArcServeRestart`; wait for 0 `llama-server`/`llama-swap` procs; `cmd /c build-llama-dll.cmd` then `build-prod-server.cmd` (vcvars64 → `cmake --build build --target llama` / `--target llama-server`); remove sentinel; `schtasks /Run /TN ArcServeBoot`; `ssh fx99 sudo systemctl start …`; wait one deep-probe tick on a QUIET rung; `query_rung_state` | a restore-across-layouts proof ON production (needs a 30B-A3B f16 ≤16k one-stream state from AM4 and the `:8082` bearer for `/slots` — the route-it lap); whether the relinked `ggml-vulkan.dll` (now from committed 242c3cd rather than the Aug 22 dirty tree) is byte-for-byte the same kernel code — the at_rate reading says it serves the same, one clean sample; fork commits local on both hosts, nothing pushed; the `build-nstream/` tree is now redundant (build 51 vs production's 52 of the same source) |
| 2026-09-19T04:50–05:40Z (cc-544e4480, SYCL-vs-Vulkan L0, desk) | Derek: look at vLLM's current B70 support and whether the SYCL backend is much faster than llama.cpp's Vulkan; design and document an /rnd workflow with what we hope to learn and how each result feeds decisions downstream. Three read-only sweeps (local estate; vLLM XPU; llama.cpp SYCL / IPEX-LLM) | **Workflow designed and landed: `docs/rnd/sycl-vs-vulkan/WORKFLOW.md` (+ `.html`); plan approved.** Findings that shaped it: (1) **vLLM XPU runs on B70 officially** (V1, `vllm/vllm-openai-xpu` since v0.26, recipes.vllm.ai validates Qwen3.5/3.6 on B70) but is **Linux-only**, no Windows wheel, and on THIS host the WSL2 dual-Arc bridge is kernel-dead (ember ADR-0012, June 2026: `dxgkrnl` mutex deadlock + NEO abort at `wddm_memory_manager.cpp:914`); dual-B70 TP needs oneCCL workarounds (vllm#41663, no P2P — dead by silicon here); NixlConnector has no XPU path; cross-vendor P/D documented nowhere → **parked by Derek's call (Windows-only)**, reopen on native Linux or a B70 back in AM4. (2) **llama.cpp PR #25222 (2026-07-15) put oneDNN/XMX flash attention into SYCL: 4.26× prefill at 80k on a B70, +90% at 128k on a dense 27B, prefill only** — exactly where our Vulkan numbers collapse (117 tok/s at 119k); against it, the one same-card comparison (jonathanmann, ~2k ctx) has Vulkan ahead on 8-stream decode 170 vs 100, and SYCL on Battlemage carries open correctness bugs (#21893 weight corruption without `GGML_SYCL_DISABLE_OPT=1`, #20169 garbled Windows output, #19276 FA corruption on Xe2 iGPU) plus #22885 (dual MoE layer-split ignores `--tensor-split` → OOM). **Nothing measures both backends' newest kernels on the same card at depth — that gap is the ladder.** (3) Vulkan has its own Xe2 FA kernels in flight (PR #24406, Intel author) — a fork rebase is a first-class arm, measured first. (4) OMEN has NO oneAPI toolkit (installer stub only), driver 32.0.101.8974; IPEX-LLM archived 2026-01-28. (5) **Date correction:** the files date the prior vLLM/SYCL attempts to May–June 2026, not ~Nov 2025 | `docs/rnd/sycl-vs-vulkan/WORKFLOW.md`, `WORKFLOW.html`; plan `~/.claude/plans/silly-orbiting-lighthouse.md`; three agent reports in the cc-544e4480 transcript; sources cited inline (vllm docs, vllm#41663, vllm-xpu-kernels, llama.cpp #25222/#24406/#21893/#20169/#19276/#22885/#21517, ember ADR-0009/0012, denning P5 2026-06-21, `docs/LEVEL-ZERO-LEVERAGE-BRIEF.md` Q6, `am4:~/am4-fleet-node/results/2026-06-29-sycl-context-ladder.md`) | read `docs/rnd/sycl-vs-vulkan/WORKFLOW.md`; ladder L1 → L1b → L2 → L3 → L4 → L5 → L6 → L7 → L8; build recipes `E:\work\llamacpp-knee\build-sycl.cmd` / `build-vk-next.cmd`; drivers now take `--omen-server` (SYCL seats on `:8096`) | L1 needs the oneAPI toolkit installed on OMEN (system install, several GB — Derek's go); whether #24406 has merged; whether SYCL's `-sm layer` loads the dual MoE at all (#22885); the SAT-L1 harness location for L5 (find via tag `prereg-np-sweep-lap1b-20260909`); `/mnt/win/work` on AM4 is NOT mounted and the second NVMe is not visible to the OS — the 4 TB lab drive may have moved physically |
| 2026-09-19T05:25–06:00Z (cc-544e4480, SYCL-vs-Vulkan L1, environment) | Can the knee fork at the production commit (`60cdd25`) build llama.cpp's SYCL backend on Windows, and do both B70s enumerate under Level Zero? | **Yes on both — the ladder is open.** But the environment had three traps before the first compile: (1) winget refused the toolkit ("already installed, no upgrade") because a ghost registry entry for oneAPI Toolkit **2026.1** pointed at the installer stub in `C:\Program Files (x86)\Intel\oneAPI\Installer`; the stub's own db said the DPC++ compiler was installed but nothing was on disk under the Intel dirs — **it had been installed 2026-09-08 to a custom dir, `E:\omen\tensor\native\toolchain\oneapi`**, below my search depth. `installer.exe --silent --action modify --components cpp-dpcpp-common:mkl.devel:dnnl:tbb.devel:dpl` (elevated) added oneMKL 2026.1 + oneDNN 2026.0 (378 MB) in ~50 s. (2) Intel's `setvars.bat` fails on this box: after its Visual Studio init every component prints `'vars.bat' is not recognized` — cmd stops resolving `call vars.bat` against the cwd once VS's chain has run — and the DPC++ vars.bat also refuses BuildTools unless `VS2022INSTALLDIR` is set and wants `vswhere.exe` on PATH. `E:\work\llamacpp-knee\sycl-env.cmd` reproduces setvars with full-path calls. (3) A Bash heredoc turned `\env\vars.bat` into `\envars.bat` (the `\v` trap) — Write, not heredoc, for anything with backslashes. Then: **`sycl-ls`: `level_zero:0` and `:1` = Arc Pro B70 (driver 1.15.39183, UR over Level-Zero V2), `:2` = the iGPU**; cmake found SYCL compiler 20260100, **oneDNN** (the #25222 XMX FA path), oneMKL 2026.1, `GGML_SYCL_SUPPORT_LEVEL_ZERO_API ON`; `build-sycl.cmd` → 431 steps clean, `ggml-sycl.dll` 66 MB; `llama-server.exe --version` = `build 52 (60cdd25)`; `--list-devices`: `SYCL0`/`SYCL1` B70 31,906 MiB (17 GB free each with production resident), `SYCL2` iGPU. Runtime hint from the binary: set `ZES_ENABLE_SYSMAN=1` for true free-memory | `E:\work\llamacpp-knee\build-sycl\bin\` (llama-server.exe, llama-bench.exe, ggml-sycl.dll); build log in the session scratchpad; installer logs `scratchpad/oneapi-modify/installer.modify.*.log` ("Product installation directory is E:\omen\tensor\native\toolchain\oneapi"); `docs/rnd/sycl-vs-vulkan/WORKFLOW.md` results section | `cmd /c E:\work\llamacpp-knee\build-sycl.cmd` (sources `sycl-env.cmd`: VS2022INSTALLDIR=BuildTools, VS Installer dir on PATH, `setvars-vcvarsall.bat`, then each component's `env\vars.bat` by full path); `cmd /c E:\work\llamacpp-knee\sycl-run.cmd build-sycl\bin\llama-server.exe --list-devices`; device pick for seats: `ONEAPI_DEVICE_SELECTOR=level_zero:0` (one B70) or `level_zero:0;level_zero:1` (both), never `:2` | L2 next (needs the maintenance window: a single-card 30B-A3B/27B seat does not fit beside production's ~15 GB per card); whether the SYCL build's `--list-devices` ordering matches `sycl-ls` under the seat (record the seat's own device lines); the oneAPI components not installed (Fortran, IPP, VTune, MPI) are not needed; `build-nstream/` is now redundant with production's `build/` |
| 2026-09-19T05:55–06:15Z (cc-544e4480, SYCL-vs-Vulkan L2, single-card sanity + correctness) | One B70, same fork commit both binaries: `llama-bench` pp512/pp2048/tg128 on the 30B-A3B and the 27B, then a cold T=0 96-token answer on the same 16k body token-for-token against Vulkan, SYCL with and without `GGML_SYCL_DISABLE_OPT=1` | **Edge: SYCL's MoE path is NON-DETERMINISTIC at T=0; the dense 27B is byte-identical across backends.** Bench (Vulkan → SYCL, tok/s): 30B-A3B pp512 2383 → 1498 (−37 %), pp2048 2170 → 1880 (−13 %), tg128 121.9 → 111.0 (−9 %); 27B pp512 756 → 892 (**+18 %**), pp2048 703 → **1111 (+58 %)**, tg128 23.9 → 21.5 (−10 %). Cold 16k, 27B: Vulkan prefill 375 tok/s / decode 15.25; SYCL **810 tok/s (2.16×)** / 12.23 (−20 %); **generation identical, 397/397 chars** (q8_0 KV, FA on, optimized kernels — no #21893/#20169 garbling). Cold 16k, MoE: Vulkan 520 tok/s / 18.85, **deterministic** (rerun identical, 458/458); SYCL **1215–1401 tok/s (2.3–2.7×)** / 18.8–20.6 — but **four SYCL runs (default ×2, DISABLE_OPT ×2) produced four different fluent answers**, 1–2 chars in common with each other and with Vulkan; `DISABLE_OPT` neither restores determinism nor changes speed. Not garbling: every SYCL MoE answer was coherent, and the first one was better grounded than Vulkan's (named `validate.py` → `inspection.py`/`envelope.py`/`authority.py`, which ARE in the 16k body; Vulkan named `hearth.operator.cli`, which is not). So: byte-equality is the wrong gate for a MoE across backends AND the SYCL MoE path is not run-to-run reproducible — that second fact is the one D1 has to weigh (replay/verify in the control plane assumes T=0 reproducibility). Depth already flips prefill: at 16k SYCL prefills the MoE 2.3× faster although pp512 was 37 % slower | `memsplice/results/cold-answer-l2-{vk,sycl}-{27b,moe}-16k-*.json` (all generations in full), `scratchpad/bench-{vk,sycl}.log` (llama-bench md tables); seat logs `hearth/var/swap-logs/l2-*-8095.log` / `l2-*-8096.log`; `bench_cold_answer.py` (new driver) | window open (fx99 stop, sentinel, `ArcServeRestart`); Vulkan: `GGML_VK_VISIBLE_DEVICES=1 build\bin\llama-bench.exe -m <30B-A3B> -m <27B> -ngl 99 -fa 1 -ub 1024 -p 512,2048 -n 128 -r 3`; SYCL: `sycl-env.cmd` + `ONEAPI_DEVICE_SELECTOR=level_zero:0 ZES_ENABLE_SYSMAN=1 build-sycl\bin\llama-bench.exe …`; seats: Vulkan `build\bin\llama-server.exe … -c 32768 -np 1 -ub 1024 -ctk q8_0 -ctv q8_0 --cache-ram 0 --port 8095`, SYCL `sycl-seat.cmd level_zero:0 8096 <log> -m … -ngl 99 -fa on --no-mmap -c 32768 -np 1 -ub 1024 -ctk q8_0 -ctv q8_0 --cache-ram 0`; `python -u tests\benchmarks\bench_cold_answer.py --omen-server http://127.0.0.1:8096 --body results\moe-body-<hash>.json --compare <vulkan json>` | whether SYCL MoE determinism returns with `-fa off` (the FA reduction) or is in the fused top-k MoE kernel — one relaunch, not done; whether Vulkan's MoE stays deterministic across `-ub` (batch-shape sensitivity is known for MoE routing); the physical-card mapping Vulkan device 1 ↔ level_zero:? (they were different cards — both seats fit at once — but which is which is unrecorded); the 30B-A3B llama-bench loss at pp512 vs the 2.3× win at 16k — the crossover length is unsampled |
| 2026-09-19T06:15–06:35Z (cc-544e4480, SYCL-vs-Vulkan L3, production's shapes) | Do production's shapes exist on SYCL: the MoE dual layer-split at `-c 131072 -np 8` (#22885: tensor-split ignored → one 25 GB allocation → OOM?), the 27B dual, and `-sm tensor` once (segfaulted on AM4 in June; P2P dead by silicon) | **All three load; #22885 does not bite at 60cdd25.** MoE dual layer-split: `-ts 1,1` honoured (SYCL0 8976 / SYCL1 8549 MiB — production's exact split), 8 slots × 16k, f16 KV 6400/5888 MiB; single-stream 400-token decode **104.9 tok/s** (run 1 94.1 with a 38 tok/s JIT warm-up prefill) vs production Vulkan 105–110 — parity. **`-sm tensor` LOADS AND RUNS** (`Meta() model buffer 8702 MiB`, graph splits = 2 — a capability Vulkan does not have): short-prompt decode 19.1 tok/s (SYCL single-card 21.5, Vulkan 23.9); 16k cold prefill 622 tok/s (SYCL single 810, Vulkan single 375), decode 12.3 (SYCL single 12.2); output matches Vulkan's for all 254 chars produced. At ≤16k the per-layer host round-trip (no P2P) cancels the parallelism; at 119k, where one token is ~200 ms of attention, it may not — L4 arm. Two `.cmd` traps on the way: cmd splits arguments on `;` and `,` — `"level_zero:0;level_zero:1"` and `-ts "1,1"` must be quoted or the seat dies before its log opens (`sycl-seat.cmd` now tees the console to `<log>.console`) | `hearth/var/swap-logs/l3-sycl-moe-dual-np8-8096.log`, `l3-sycl-27b-tensor-8096.log` (+ `.console`); `memsplice/results/cold-answer-l3-sycl-27b-tensor-16k-*.json` | `sycl-seat.cmd "level_zero:0;level_zero:1" 8096 <log> -m <30B-A3B> --alias qwen3-30b-a3b -ngl 99 -sm layer -ts "1,1" -fa on --no-mmap -c 131072 -np 8 -ub 1024 --cache-ram 0`; tensor: `… -m <27B> -ngl 99 -sm tensor -fa on --no-mmap -c 32768 -np 1 -ub 1024 -ctk q4_0 -ctv q4_0 --cache-ram 0` | tensor split at depth (L4); `-np 8` throughput on SYCL at 16k (L5) — parity single-stream says nothing about the batch; the 27B dual layer-split loads (launched next as the L4 seat) |
| 2026-09-19T06:35–07:05Z (cc-544e4480, SYCL-vs-Vulkan L4, the depth ladder) | Does the #25222 claim hold on our B70s — SYCL/XMX prefill of the dense 27B holding its rate to 120k where Vulkan collapses — and what does decode do at depth? Same shape as lap 9 (27B, q4_0 KV, both B70s, `-c 131072`), same prompt bodies; plus the tensor-split arm at 119k | **It holds; the knee is gone; and tensor split moves the depth-decode ceiling.** SYCL dual layer-split, prefill tok/s at 16k / 30k / 60k / 119k: **813 / 817 / 736 / 614** (Vulkan dual: 609 / 443 / — / 117) — 119k prefills in **194 s vs 1,015 s, 5.2×**; decode 14.0 / 10.5 / 7.0 / 4.24 (Vulkan 15.2 / 11.4 / — / 4.9: −8 to −13 %). **`-sm tensor` at 119k: prefill 482 tok/s (4.1× Vulkan), decode 6.74 tok/s — +59 % over SYCL layer-split, +38 % over Vulkan** — both cards on every layer's attention finally pays where a token costs ~200 ms; output byte-identical to the layer-split generation (254/254) and coherent (`hearth/backlog/__main__.py`). D2 consequence: the disaggregated pipeline (AM4 81.5 + wire 20 + restore 0.9 ≈ 103 s) is now 1.9× SYCL-local at 119k, not 9.8× — the route threshold moves, but the pipeline's rationale on the throughput scoreboard (194 s of B70 prefill is 194 s of no decode) stands; the B70s CAN prefill at depth now at 42 % of AM4's rate. D3: the 27B depth specialist has its engine candidate — SYCL tensor-split | `memsplice/results/cold-answer-l4-sycl-27b-dual-{16k,30k,60k,119k}-*.json`, `cold-answer-l4-sycl-27b-tensor-119k-*.json`; seat logs `hearth/var/swap-logs/l4-sycl-27b-{dual,tensor}-128k-q4-8096.log`; Vulkan comparators: lap 7 (`moe-longctx-27b-dualb70-*`), lap 9 (`omen-depth-119k-q4-dualb70-*`) | window open; `sycl-seat.cmd "level_zero:0;level_zero:1" 8096 <log> -m <27B> -ngl 99 -sm layer -ts "1,1" -fa on --no-mmap -c 131072 -np 1 -ub 1024 -ctk q4_0 -ctv q4_0 --cache-ram 0 --slot-save-path E:\work\battlemage\kv\` (tensor arm: `-sm tensor`, no `-ts`); `python -u tests\benchmarks\bench_cold_answer.py --omen-server http://127.0.0.1:8096 --body results\{moe-body-d5149648f7b23172,ceiling-body-61c18f245bcaae9a,ceiling-body-6fd63cf35a0120e8,ceiling-body-705bb11bc87f92a5}.json --n-predict 64`; close: `ArcServeBoot` + fx99 timers | L5 (jobs/h at -np 8 × 16k on SYCL — the D1 unit; needs the SAT-L1 harness); L6 (eight 119k contexts on SYCL TENSOR split — the aggregate ceiling may move with it); L7 (the CUDA-made q4_0 state restored into a SYCL seat); tensor split's prefill cost (482 vs 614) at depth and whether `-ub 2048` recovers it; Vulkan single-card 60k/119k numbers were never measured (dual only) |
| 2026-09-19T06:45–07:25Z (cc-544e4480, SYCL-vs-Vulkan L4b + L7, the 256k question) | Derek: can the 27B run at 256k on the B70s, and can 256k prefill in ~3 min on Intel ("a miracle — stop, document, celebrate"; AM4 in the mix allowed; a little over 3 still excellent)? Seat: SYCL, both B70s, layer split, q4_0 KV, `-c 262144`. Two clocks: SYCL alone on a 248,515-token body; AM4's 119k state restored + a 121k tail on the B70s | **256k loads and serves (2.3 GB KV per card, 2.4 GB compute); it is minutes, not the hour-plus Vulkan's knee implies — but not three. SYCL alone: 248,515 tokens in 552.9 s = 9.2 min (449 tok/s cumulative; 614 at 119k, so it slows past there), decode 2.24 tok/s at 248k, correct answer. Hybrid: 240,312 tokens in 433 s = 7.2 min** = AM4 81.5 s (119k, lap 8) + wire 20.0 s + restore 1.2 s + **B70 tail 121,109 tokens at 366 tok/s (330.7 s)**; correct answer. **L7 answered for free: the CUDA-made q4_0 slot state restores into a SYCL seat** (`n_restored=119203`, 1,216 ms) and the continuation is right — the state file is vendor- AND backend-agnostic. The wall is the tail: past 119k every token costs the B70s ~2.7 ms and AM4 cannot carry it (its 12 GB pair caps at 128k). **Path to the 3-minute bar (not tonight):** a smaller weight quant on AM4 (IQ4_XS ≈ 15 GB) fits 256k of q4_0 KV on that pair → the whole prompt at ~1,200 tok/s ≈ 3.5 min, the 5 GB wire hidden under it once the streaming detour exists. Also unmeasured: `-ub 2048 -b 4096` on the tail (the B60 SYCL lever, +10–30 %) | `memsplice/results/cold-answer-l4b-sycl-27b-dual-250k-*.json`, `split-prefill-l4b-hybrid-am4-119k-plus-sycl-tail-*.json`, body `ceiling-body-300dd996b100b233.json` (248,508 tok from 105 of 301 files in `commandcenter/hearth/**/*.py`); seat log `hearth/var/swap-logs/l4b-sycl-27b-dual-256k-q4-8096.log` | window; `sycl-seat.cmd "level_zero:0;level_zero:1" 8096 <log> -m <27B> -ngl 99 -sm layer -ts "1,1" -fa on --no-mmap -c 262144 -np 1 -ub 1024 -b 2048 -ctk q4_0 -ctv q4_0 --cache-ram 0 --slot-save-path E:\work\battlemage\kv\`; `python tests\benchmarks\build_body.py --tokens 250000 --glob "C:/work/commandcenter/hearth/**/*.py" --server http://127.0.0.1:8096`; `python -u tests\benchmarks\bench_cold_answer.py --omen-server http://127.0.0.1:8096 --body results\ceiling-body-300dd996b100b233.json --n-predict 32`; hybrid: `python -u tests\benchmarks\bench_split_prefill.py --omen-server http://127.0.0.1:8096 --body results\ceiling-body-705bb11bc87f92a5.json --filename qwen38-27b-q4kv.0.705bb11bc87f92a5.bin --glob "C:/work/commandcenter/hearth/**/*.py" --tail-tokens 129000 --n-predict 32` | `-ub 2048 -b 4096` on the deep tail; tensor split at 256k (decode); Vulkan's actual 256k time (extrapolated only); the IQ4_XS AM4 seat (the 3-minute path); answer quality at 248k beyond one correct paragraph |
| 2026-09-19T07:35–08:25Z (cc-544e4480, SYCL-vs-Vulkan L4c, f16 KV + ubatch) | Derek: does the new tooling unlock anything untried? Reading `fattn.cpp` at 60cdd25: the oneDNN/XMX path is prefill-only (≥32 query tokens), every decode step is TILE/VEC; with quantized KV the oneDNN path dequantizes the WHOLE cache to f16 per ubatch and the VEC decode kernel dequantizes per step; f16 KV is "native SDPA at any KV length". Probe: f16 KV on the SYCL dual 27B seat at `-c 262144`, then `-ub 2048 -b 4096` and `-ub 4096 -b 8192`, same 119k / 248k bodies | **Edge: f16 KV lifts decode at depth 2.2–2.6× and costs nothing on prefill.** 119k: prefill 597 tok/s (q4_0: 614), **decode 9.41 tok/s (q4_0 SYCL 4.24, Vulkan 4.9)**; 248k: prefill 435 (q4_0: 449), **decode 5.89 tok/s (q4_0: 2.24)**. So the per-step dequant was the decode-at-depth wall, and the per-ubatch cache conversion was NOT the deep-prefill slide (a clean negative). Memory: 8,192 MiB KV per card at 256k — fits with ~10 GB to spare per card. Then the ubatch knob on prefill: `-ub 2048 -b 4096` **694 tok/s at 119k (+16 %)**, `-ub 4096 -b 8192` 713 (+3 % more; compute buffer 9.5 GB/card, still fits); decode unchanged. **Best shape: 248,515 tokens in 512.7 s = 8.5 min at 485 tok/s, decode 5.89 tok/s, correct answer** — from 9.2 min / 2.24 tok/s at the start of the night, on the same silicon. T=0 outputs are reproducible for a fixed (backend, ubatch) but change with `-ub` (10 common chars between 1024 and 2048 at 119k) — batch-shape sensitivity, note for any determinism policy. Still not Derek's 3-min bar; the levers left for prefill are MTP (decode), the oneDNN-for-decode gate (fork patch), and the four-GPU RPC pipeline (structural) | `memsplice/results/cold-answer-l4c-sycl-27b-dual-{119k-f16,250k-f16,119k-f16-ub2048,119k-f16-ub4096,250k-f16-ub4096}-*.json`; seat logs `hearth/var/swap-logs/l4c-sycl-27b-dual-256k-f16{,-ub2048,-ub4096}-8096.log`; kernel-selection source `ggml/src/ggml-sycl/fattn.cpp:95-175`, `fattn-onednn.cpp:30-80` | window; `sycl-seat.cmd "level_zero:0;level_zero:1" 8096 <log> -m <27B> -ngl 99 -sm layer -ts "1,1" -fa on --no-mmap -c 262144 -np 1 -ub 4096 -b 8192 --cache-ram 0 --slot-save-path E:\work\battlemage\kv\` (no -ctk/-ctv = f16); `python -u tests\benchmarks\bench_cold_answer.py --omen-server http://127.0.0.1:8096 --body results\ceiling-body-300dd996b100b233.json --n-predict 64` | f16 KV on TENSOR split at depth (6.74 was q4_0 — f16 tensor may stack the two gains); f16 KV on Vulkan (never tried on the B70s either — the VEC dequant may be Vulkan's wall too); MTP at depth; the oneDNN decode gate; RPC four-GPU pipeline; q8_0 KV as the middle ground (memory vs decode); the MemSplice file at f16 (64 KB/token → 2× the wire) |
| 2026-09-19T08:30–08:55Z (cc-544e4480, desk: LEVERS-256K inventory) | Derek: another pass over our tools and untried levers for the 256k 27B objective — fx99, OpenVINO, the NPU included; inventory what exists instead of saying no | **Inventory landed: `docs/rnd/sycl-vs-vulkan/LEVERS-256K.md` (+ `.html`).** New facts found on the pass: (1) **OMEN already has a Marvell AQtion 10GbE NIC**, linked at 1 Gbps because AM4's I211 is 1 GbE — a $30–100 NIC on AM4 makes the wire 2.5–10× (an f16 256k state in ~16 s); (2) **the fork ships `ggml-rpc` and `ggml-openvino` backends, both OFF in every build** — RPC = a four-GPU layer-split pipeline across AM4's pair and the B70s over the direct link (AM4's 128k ceiling stops mattering; prefill scales with depth); ggml-openvino reaches the **NPU** (static, stateless, ≤8B-class, 256-token prefill chunks) — a draft-device candidate, not a 27B engine; (3) the fork's speculative family is all present: `draft-mtp` (and **`mtp-Qwen3.8-27B-Q4_0.gguf` is on disk**), `eagle3`, `dflash`, and **n-gram drafts that need no model at all**, with `-devd` to put a draft on the iGPU/CPU; (4) OpenVINO 2026.3 sees CPU/GPU.0/GPU.1/GPU.2/NPU; Qwen3.8-27B needs GenAI 2026.4+ (community: 13.9 tok/s short decode on a B70 — below llama.cpp); (5) fx99 = one 2070 SUPER 8 GB — cannot hold the 27B, could join an RPC pipeline for a few layers. Ranked: B1 f16+tensor, B2 MTP, B3 n-gram (one window) → A1 RPC four-GPU (~1 h) → A3 10 GbE + A2 IQ4_XS on AM4 (the ~3.5-min path) → B4/A7 only if needed | `docs/rnd/sycl-vs-vulkan/LEVERS-256K.{md,html}`; sources: `Get-NetAdapter`, `llama-server --help` (fork), `ggml/src/` tree, `docs/backend/OPENVINO.md`, `knowledge/fx99_gpu_catalog.json`, `fleet/inventory.toml`, the npuw venv's `ov.Core().available_devices`, HF `SergiioB/Qwen3.8-27B-int4-gdn8-ov` | read `LEVERS-256K.md`; first window: `sycl-seat.cmd … -sm tensor` with f16 KV, then `--spec-type draft-mtp -md E:\workattlemage\models\qwen38\mtp-Qwen3.8-27B-Q4_0.gguf --spec-draft-n-max 3`, then `--spec-type ngram-cache`, on the 119k/248k bodies | every lever in the document is untested on this fleet except A4/A5; whether the 10.44.0.1 link rides the AQtion port or the I226-V (check before buying a NIC); RPC on a Windows client; T=0 equivalence under speculation |
| 2026-09-19T08:05–08:57Z (cc-544e4480, L4d partial + INCIDENT) | L4d: f16 KV on tensor split; MTP; n-gram — decode-at-depth levers with no new hardware. Then Derek's denning detour and a production outage inside it | **B1 measured:** f16 + tensor split at 119k → decode **10.24 tok/s** (layer f16 9.41, +9 %), prefill 555 (layer 713, −22 %). The two gains do NOT stack — with f16 the tensor advantage falls from +59 % to +9 % (halving the dequant was most of it). Layer split + f16 stays the all-round seat. **B2 (MTP) — first attempt INVALID:** the MTP draft on top of 256k f16 `-ub 4096` pushed one B70 over 32 GB and **WDDM spilled 24.8 GB to shared system memory silently** (31.6 GB dedicated + 24.8 shared on one card; the `llama-server` at 90 GB commit; GPU at 0 %) — denning's spill cliff, live; Derek spotted 91 GB of system RAM in Task Manager. `E:\work\llamacpp-knee\gpu-mem-gate.ps1` written (dedicated at the 32 GB ceiling AND shared beyond the ~4–8 GB SYCL host buffer = spilled); relaunched MTP at `-c 131072 -ub 2048` (15–20 GB/card, gate clean) — run interrupted for the denning detour, NOT measured. **INCIDENT:** production would not come back after the window — two llama-swap loads died at the 5-min health timeout with the cards empty and RAM free. I blamed post-spill WDDM state and had Derek approve `pnputil /restart-device` on both B70s (clean, changed nothing). The cause was **a runaway `grep -rniE "software tax" /e/work/{denning,b70tools,battlemage}` left behind by the denning Explore agent — `battlemage/` holds the models directory — reading E: at 350 MB/s for ~20 min (538 CPU-s); the `-dio` model load starved behind it.** `\Process(*)\IO Read Bytes/sec` named it in one call; killed → E: 3 MB/s → production up in 10 s. Denning's findings (agent report, in the transcript): the demotion cliff is sharp at the VidMm budget (~31.1 GB/card), decode collapses 4–6× and saturates while spill grows, priority/pinning cannot defend a process (admission control must), `-fit on` is the flag that overcommits silently, f16 KV to 128k did NOT spill single-stream, restore beats re-prefill 114–475×, and — new to me — b70tools' PDH `non_local` is nearly blind under SYCL/Level Zero (why one B70 read 0 dedicated tonight) | `memsplice/results/cold-answer-l4d-sycl-27b-tensor-119k-f16-*.json`; seat logs `hearth/var/swap-logs/l4d-*`; `hearth/var/arc-swap.log` (the two 5-min timeouts), `arc-serve.log` (69 s Vulkan0 upload); `E:\work\llamacpp-knee\{gpu-mem-gate.ps1,reset-b70s.ps1,reset-b70s.log}`; memory `feedback-check-the-disk-before-blaming-the-gpu` | B1: `sycl-seat.cmd "level_zero:0;level_zero:1" 8096 <log> -m <27B> -ngl 99 -sm tensor -fa on --no-mmap -c 262144 -np 1 -ub 4096 -b 8192 --cache-ram 0`; MTP (budgeted): `… -sm layer -ts "1,1" -c 131072 -ub 2048 -b 4096 --spec-type draft-mtp -md E:\workattlemage\models\qwen38\mtp-Qwen3.8-27B-Q4_0.gguf --spec-draft-n-max 3` then `gpu-mem-gate.ps1` (or `b70tools verdict --json`) BEFORE the run; outage triage: `Get-Counter '\PhysicalDisk(1 e:)\% Disk Time'` then `'\Process(*)\IO Read Bytes/sec'` | MTP and n-gram at depth (B2/B3) — unmeasured; the gate's thresholds vs b70tools' verdict (use b70tools); whether the FIRST slow load (before any grep existed? the grep started 08:37, the first timeout was 08:33–08:38) was also disk — the 59 % E: in Derek's 08:20 screenshot predates the grep and is unexplained; whether the earlier f16 `-ub 4096` (26.6 GB/card) seat had already partially spilled (its 713 tok/s says no, but the counters were never read) |
| 2026-09-19T09:58–10:22Z (cc-544e4480, L4d resumed: B2 MTP + B3 n-gram at depth) | Back in the saddle: the two cheapest untried decode levers, on the budgeted layer-split f16 SYCL seat (`-ub 2048 -b 4096`), spill-gated before every run | **Edge: MTP doubles decode at depth and is exact; n-gram drafting is a clean negative.** 119k, `--spec-type draft-mtp -md mtp-Qwen3.8-27B-Q4_0.gguf --spec-draft-n-max 3`: **decode 19.98 tok/s** (no spec 9.41 → **2.1×**), 88/116 drafts accepted (76 %), prefill 649; the generation matches the non-speculative one for every character (279/279 vs the 64-token reference, and n-gram's 128-token output — which is the greedy path — matched MTP's 471/471). **248k: decode 14.37 tok/s** (no spec 5.89 → **2.4×**), 87/117 accepted, prefill 442 tok/s (562.8 s), correct answer; seat 21.3 / 28.4 GB dedicated, gate clean. `--spec-type ngram-cache` at 119k: **6.30 tok/s — worse than none** (17/145 accepted; explanatory prose does not copy the prompt; rejected verifies cost more than they save). Tonight's depth-decode arc on the same two cards: 4.24 (q4_0, start) → 9.41 (f16) → **19.98 (f16 + MTP) at 119k**; 2.24 → 5.89 → **14.37 at 248k**. Vulkan's numbers at those depths: 4.9 and ~1 | `memsplice/results/cold-answer-l4d-sycl-27b-dual-119k-f16-{mtp,ngram}-*.json`, `cold-answer-l4d-sycl-27b-dual-250k-f16-mtp-*.json` (timings carry `draft_n` / `draft_n_accepted`); seat logs `hearth/var/swap-logs/l4d-sycl-27b-dual-{128k,256k}-f16-{mtp,ngram}*-8096.log` | window (fx99 stop → sentinel → `ArcServeRestart`); `sycl-seat.cmd "level_zero:0;level_zero:1" 8096 <log> -m <27B> -ngl 99 -sm layer -ts "1,1" -fa on --no-mmap -c 262144 -np 1 -ub 2048 -b 4096 --cache-ram 0 --spec-type draft-mtp -md E:\work\battlemage\models\qwen38\mtp-Qwen3.8-27B-Q4_0.gguf --spec-draft-n-max 3`; `gpu-mem-gate.ps1` (or `b70tools verdict`) BEFORE the run; `python -u tests\benchmarks\bench_cold_answer.py --omen-server http://127.0.0.1:8096 --body results\ceiling-body-300dd996b100b233.json --n-predict 128`; close (`ArcServeBoot`, fx99 start) | `--spec-draft-n-max` 4–6 at depth (acceptance was 76 % at 3 — more draft may pay); MTP on the production MoE (30B-A3B has its own MTP head? — none on disk) and on Flash-Next; MTP + tensor split (tensor gave +9 % at f16; may add); n-gram on a code-completion task rather than prose; the 28.4 GB dedicated + 9.6 GB shared reading on the 256k MTP seat — under the gate but nearer the ceiling than any clean seat tonight, and the prefill rate (442) says it did not spill; decode equivalence under MTP is proven to 128 tokens at T=0 only |
| 2026-09-19T10:35–10:55Z (cc-544e4480, L4e: Derek's research list — asymmetric KV) | Derek's research list (try everything): asymmetric KV — (a) `-ctk q8_0 -ctv q4_1`, (b) `-ctk f16 -ctv q4_0` — on the layer-split SYCL 27B seat at `-c 262144 -ub 2048`, 119k body, vs f16 (9.41 decode / 694 prefill) and q4_0 (4.24 / 614) | **Asymmetric KV is a memory lever, not a speed lever, on SYCL at depth.** (a) q8_0/q4_1: **decode 5.84 tok/s**, prefill 700 (unhurt), KV **3,456 MiB/card at 256k** (f16 8,192; q4_0 2,304), output diverges from f16's after 29 chars. (b) f16/q4_0: decode **5.80** (no better), **prefill 392 tok/s (−44 %)** — mixed f16/quantized KV fails the oneDNN path's type gate (`fattn-onednn.cpp`: f16 K is not in the non-F16 branch's allowed list) and falls to a slower path; KV 5,248 MiB/card; output stays with f16's for 200 chars (K precision does track accuracy). So the VEC decode kernel's per-step cost is not dominated by K vs V symmetrically: only full f16 reaches 9.41; any quantized side lands ~5.8. Use (a) when VRAM is the constraint (e.g. many resident 256k contexts), f16 when speed is. Also found on the pass: **Flash-Next on disk is the ~130 GB three-shard model and shard 1 is a 0-byte file** — it cannot load; the 35B-A3B in the research note is a different model, not on disk | `memsplice/results/cold-answer-l4e-sycl-27b-dual-119k-{k8-v41,kf16-v40}-*.json`; seat logs `hearth/var/swap-logs/l4e-*`; gate output in the transcript (16.5/16.9 GB dedicated for (a)) | `sycl-seat.cmd "level_zero:0;level_zero:1" 8096 <log> -m <27B> -ngl 99 -sm layer -ts "1,1" -fa on --no-mmap -c 262144 -np 1 -ub 2048 -b 4096 -ctk q8_0 -ctv q4_1 --cache-ram 0` (or `-ctk f16 -ctv q4_0`); gate; `bench_cold_answer.py --omen-server http://127.0.0.1:8096 --body results\ceiling-body-705bb11bc87f92a5.json --n-predict 64` | q8_0/q8_0 on SYCL (the symmetric middle — likely ~5.8 too, unmeasured); asymmetric + MTP (MTP's 2.1× should apply on top of 5.84); the same three KV types on Vulkan (never measured there either); the rest of the list: AOT build (`GGML_SYCL_DEVICE_ARCH=bmg`), RPC four-GPU, MoE+MTP (needs a model download — Flash-Next shard 1 or the 35B-A3B) |
| 2026-09-19T11:00–11:35Z (cc-544e4480, L4f: RPC four-GPU pipeline + AOT build) | Derek's research list, items 2 and 4: (a) `GGML_RPC=ON` on both hosts — OMEN's SYCL server with AM4's 4070 Ti + 5070 as `ggml-rpc` devices over the 1 GbE direct link, the 27B layer-split across all four GPUs; (b) `GGML_SYCL_DEVICE_ARCH=bmg-g31` (spir64_gen AOT) vs the JIT build, llama-bench on one B70 | **(a) The four-GPU, two-host model WORKS; on 1 GbE it is a decode lever and a prefill loss.** `--rpc 10.44.0.2:50052` (+ `-dev "RPC0,RPC1,SYCL0,SYCL1" -ts "1,1,1,1"`, f16 KV, `-c 131072 -ub 2048`): loads in 96 s streaming 8.5 GB of weights over the link (29 s on reload with the rpc-server's `-c` local cache); each device ~4.1–4.8 GB weights + 2 GB KV; NVIDIA cards at 7.5 GB of 12. 119k body, **two rpc-servers (one per card): prefill 272 tok/s, decode 11.70**; **one rpc-server exposing both cards: prefill 498 tok/s, decode 11.71** (the RPC0→RPC1 hop was client-mediated — two extra wire trips per ubatch). vs the dual B70s alone: prefill 694, decode 9.41. So: **decode +24 %** (the NVIDIA cards run their 32 layers faster; ~60 KB per token on the wire), **prefill −28 %** (activations cross as f32: ~42 MB per 2048-token ubatch, and `device RPC0 does not support async, host buffers or events` — no pipeline overlap on RPC devices). Output identical run-to-run (259/259). Traps: `ggml-rpc-server` is the target name (not `rpc-server`); AM4's ufw needed `allow from 10.44.0.1 to any port 50052:50053 proto tcp`; `.cmd` files need CRLF for `goto` labels; Git Bash strips the inner quotes on comma lists (launch via PowerShell). **(b) AOT = JIT within noise**: pp512 882 vs 887, pp2048 1107 vs 1113, tg128 21.41 vs 21.42 — AOT only removes first-run JIT time. Clean negative | `memsplice/results/cold-answer-l4f-rpc4{,b}-27b-119k-f16-*.json`; seat logs `hearth/var/swap-logs/l4f-rpc4{,b}-27b-128k-f16-8096.log`; `am4:~/work/am4-dual-nvidia-poc/logs/rpc-{cuda0,cuda1,both}.log`; `scratchpad/bench-aot.log`; builds `E:\work\llamacpp-knee\build-sycl-rpc\`, `build-sycl-aot\`, AM4 `build/bin/ggml-rpc-server` | AM4: `ggml-rpc-server -d CUDA0,CUDA1 -H 10.44.0.2 -p 50052 -c` (stop the :18090 seat first); OMEN: `sycl-rpc-seat.cmd 8096 <log> -m <27B> -ngl 99 -sm layer -dev "RPC0,RPC1,SYCL0,SYCL1" -ts "1,1,1,1" -fa on --no-mmap -c 131072 -np 1 -ub 2048 -b 4096 --cache-ram 0` (via PowerShell Start-Process); `bench_cold_answer.py --omen-server http://127.0.0.1:8096 --body results\ceiling-body-705bb11bc87f92a5.json --n-predict 64`; AOT: `build-sycl-aot.cmd bmg-g31` then `scratchpad\bench-aot.cmd` | RPC + MTP (decode 11.7 × ~2 → the best depth-decode seat on the fleet, prefilled elsewhere); restore a saved state INTO the four-device cache (the KV is split per layer across devices — the across-layouts patch handles streams, device placement untested) → prefill on AM4/B70s, decode on four; `-ts` weighted toward the NVIDIA cards for decode; fx99 as a fifth RPC device (its own ufw + LAN hop); f16 activations over the wire (not configurable); any prefill win from RPC needs the 10 GbE that is not in the cards |
