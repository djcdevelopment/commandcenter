# Capacity-aware dispatch: 2026-09-21 overnight cycle 3

Cycle ceiling: 07:57:34–08:17:34 UTC. First report cutoff 08:02:34; generation
cutoff 08:12:34; final five minutes reserved for review and delivery. The initial
report was written at 08:02:50, 16 seconds late. No additional model run is needed.

## First useful finding

The current scheduler can place work in an existing inference slot, but cannot
justify placing an additional model on a GPU: `hearth/toolsurface/jev_scheduler.py`
`capture()` supplies empty `hosts`, `gpus`, and `models` maps and a null
`free_vram_gb`. Idle slots are not free VRAM.

The existing operator sources are already located and should be reused:
`hearth/operator/paths.py` resolves profiles and harnesses under `hearth/etc`,
inventory under `fleet/inventory.toml`, and GPU catalogs under `knowledge`.
`knowledge/capacity_estimates.json` is historical performance evidence, not a live
memory measurement or a guaranteed context limit. A maximum observed context is
not the model's configured context capacity.

The B70 observer also needs careful interpretation. The local
`E:/work/b70tools/docs/retrospective-bsod-fix-and-sycl-unlock-2026-06-18.md`
records PDH substantially under-reporting SYCL allocations; a small PDH reading
cannot alone certify available VRAM. Its older operational runbook identifies
unreliable IGCL fields. DXGI process budgets must not be relabeled as adapter-wide
free memory. Neither Windows Task Manager nor one low counter proves spill or
absence of spill.

## Actual observations, not capacity promises

| Observation | Value | Source and limitation |
| --- | --- | --- |
| OMEN resident worker at 08:03:07 UTC | `qwen3-30b-a3b`, ready, 8 total slots / 0 busy / 8 free, 16384 tokens **per slot** | `fleet/hermes/serve-hearth.py --probe-worker`; native runtime, not advertised aggregate context |
| AM4 5070 at approximately 08:03 UTC | 12227 MiB total, 88 used, 11685 free; utilization 0% | `nvidia-smi`; driver-reported free is intentionally not recomputed as total minus used |
| AM4 4070 Ti at approximately 08:03 UTC | 12282 MiB total, 15 used, 11862 free; utilization 0% | `nvidia-smi`; a free-memory sample does not establish model/KV/compute-buffer fit |
| AM4 review helper | owner `null`, model resident `false`, native context `null`, KV enabled `false` | Existing `review_seat.py status`, same observation window |
| B70 at BDF `0000:04:00.0` | PDH 14.58 GiB dedicated committed, 13.2 MiB non-local committed | b70tools three-tick capture ending 08:03:55; one slow memory sample, not a trend |
| B70 at BDF `0000:09:00.0` | PDH 15.44 GiB dedicated committed, 327.5 MiB non-local committed | Same capture; preserve LUID/BDF instead of relying on Task Manager numbering |
| Both B70s, DXGI observer process | 4 KiB local usage each; local budget about 31.04 GiB each | **Not** total adapter usage or free VRAM; same distinction applies to the observer's Vulkan heap usage |

The native observer found four adapters (including iGPU and software adapter),
not four B70s. Its B70 identity reports about 31.79 GiB dedicated memory per
card. It also emitted `impossible_heap_budget` disagreements: the raw evidence
is retained, and no free-memory admission number is derived from those budgets.
These samples do not establish inference spill, absence of spill over a workload,
or performance causation. A low shared-memory observation is not a guarantee.

Raw bounded extraction: [B70 observations](evidence/20260921-capacity-b70.json).
Full local capture: `runs/operator/capacity-20260921-0804/events.jsonl`, SHA256
`147ecb846efbe3b560b539848c358547e98d257997fccac02f8376c3131b9781`.
Capture command: `E:/work/b70tools/build/b70tools.exe run --ticks 3 --out
C:/work/commandcenter-jev-scheduler/runs/operator/capacity-20260921-0804`.
This used the existing binary; no driver, model, collector, or service change.

## Actionable recommendation

Dispatch short, source-packed work to the resident OMEN worker now, with a task
budget of at most 8192 tokens to leave room within the measured 16384-token slot
for harness instructions, tools, and output. Recheck native readiness/free slots
at dispatch; these recorded observations are historical immediately afterward.
Keep AM4 unloaded between required reviews. Do not load an additional model based
on free-slot counts, observer-process budgets, or historical capacity estimates.

This route was actually exercised: the local memo used 6851 input / 729 output
tokens and completed in 23.484 seconds, without model rotation or AM4 use. It
needed factual correction, so this is evidence of useful **assisted analysis**,
not an independently correct report or an accepted autonomous patch.

### Which existing route should do what?

| Route | Available mechanism | What this cycle establishes |
| --- | --- | --- |
| Direct Hearth | `local_generate(files=..., task_id=...)`, ledgered backend/model/job | Best immediate path for bounded extraction/drafting; exercised on resident OMEN, with source review afterward |
| MechNet build via JEV | `scheduler_prepare` / `scheduler_select`, fixed `cc-builder-2`, isolated candidate, manual promotion | Already exercised in preceding cycles. JEV currently ranks approved tasks for **one admitted profile**, not hardware layouts or three interchangeable routes |
| DeepAgents | Registry entry `deepagents_hearth`; harness at `C:/work/deepagents-poc` | Historical route evidence exists, but was not re-qualified here. Its related `deepagents-run-flash-dense` harness is marked BUILT NOT DEPLOYED. Do not infer live readiness or scheduler admission from a registry label |

The general operator package already has field-specific freshness, null reasons,
immutable snapshot identities, and historical catalogs. Reuse these contracts
when adding observations. `hearth/operator/inspection.py` explicitly leaves live
GPU `free_vram_gb` null too: switching from the narrow JEV capture to the general
capture alone would **not** solve GPU memory observability.

The next implementation lever is a bounded, per-adapter observation adapter that
preserves source, timestamp, scope (process/adapter), and disagreement/unknown
status. Expose it for planning before authorizing new model placement. Do not
turn PDH or DXGI alone into a B70 SYCL free-memory gate. That implementation is
not claimed as delivered by this reporting cycle.

## Local-model contribution and independent corrections

[Original local memo and metadata](evidence/20260921-capacity-local-memo.json):
`omen-arc`, `qwen3-30b-a3b`, `routed_by=pinned:omen-arc`, job
`job_7659232a0d7a8546191a15003512b5bf`, request
`req_8a0cd3a070ad528d935272e385c505d5`. One call; no retry.

Codex checked it against source and corrected the following:

- It invented `C:/work/commandcenter-jev-scheduler/.hearth`. The pilot's
  `fleet/jev/local.py:environment()` explicitly sets private queue/operator state
  under `C:/Users/derek/.fleet-scheduler`; the generic path defaults are separate.
- It described evidence as static while citing a live native probe. Native slot
  readiness is dynamic; historical model outcomes are not current capacity.
- `knowledge_digest()` hashes selected content; it does not independently
  authenticate or validate the knowledge corpus.
- `_prepare()` and `_select()` explicitly compare task context with the native
  slot limit, with `_select()` refreshing the native probe. The report wrongly
  assigned that specific check to `route()`/`validate_proposal()`.
- Eight free slots are an instantaneous count of request capacity, not a context
  quantity, a reservation, or permission to start eight long-running builders.
- The pilot does not choose a model via `scheduler_select(model=...)`; that API
  takes candidate/snapshot/evidence identifiers and dispatches its fixed route.

No JEV API request or Hermes review was made for this cycle; there was no reason
to spend on a second opinion after these source mismatches were identified.
The existing inference route produced a report, not a verified hardware planner.

## State and boundaries

No benchmarks, synthetic tests, test files, model swaps, production restarts, or
automatic promotion. AM4 remained unloaded; OMEN resident inference continued.
The CPU scheduler was not stopped or reconfigured. The earlier gateway-restart
restriction remains: the required-execution-evidence gate is committed source,
not loaded code. No bypass was attempted.

The current API ledger is unchanged by this cycle: six attempts, 3500 confirmed
input tokens, USD 0.000147000 configured-price usage estimate plus
USD 0.005505024 uncertain reservations. No new API spend; Codex effort and local
power are not included in that ledger.
