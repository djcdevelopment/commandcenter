Continue the authorized capacity build with one narrowly bounded, source-packed retry. The first pair produced no deliverables after spending their budgets reading; do not repeat that scope. Codex will integrate and review this small helper; your builders only produce the helper and its tests. Create ONE new HEARTH receipt and dispatch it once, then return its IDs in four lines. No probes or polling.

create_build_request: repo C:/work/commandcenter-hermes-fx99; title "Hermes source-packed native capacity helper"; backend am4-dense; task coding; execute false; deliverables ["native_capacity.py","test_native_capacity.py","CAPACITY-REVIEW.md"]; acceptance_criteria ["Pure native readiness helper rejects stale/stopped/wrong-model responses and never multiplies one physical AM4 slot by alias count.","New stdlib unittest cases pass, with GPU placement explicitly unknown; no service or default runner changes."]

Use the BUILD BRIEF below as the request. Then execute_build_request using the new receipt ID, mode delegate, backend am4-dense, builders ["cc-builder-2","cc-builder-3"], max_age_s 600, task_class build, promotion_policy manual, runner_preset am4-shared-27b. Stop after returning actual IDs or exact refusal.

BUILD BRIEF:
Implement only native_capacity.py (pure stdlib helper), test_native_capacity.py (unittest), CAPACITY-REVIEW.md (short exact result). This is a SOURCE-PACKED leaf, not a repository exploration. Codex owns integration into the larger scheduler. Do not scan directories or read whole files. If you need a source check, the ONLY required read is `sed -n '282,290p' /home/claude/hermes-capacity-source-20260919/hearth/toolsurface/scheduler.py` (baseline b2b7341). Then WRITE YOUR HELPER IMMEDIATELY, write tests, run `python3 -m unittest test_native_capacity.py`, write the result, finish. Budget is 410 seconds including shared-model waits; target a first file before 90 seconds. No additional source research, no network, no services/GPU calls, no global configuration changes.

Contract: normalize_am4_native(payload, observed_at, now=None) returns a dictionary. now defaults to current UTC datetime. A valid timestamp is timezone-aware ISO8601; observed_at must be within [-5,+30] seconds age. The authenticated facade GET /oxen/ready?alias=am4-dense-27b yields this payload:
{"all_ready":true,"aliases":[{"alias":"am4-dense-27b","ready":true,"context_length":131072,"parallel_slots":1,"physical_resource":"am4:127.0.0.1:18090","model":"models/Qwen3.8-27B-Q4_K_M.gguf","status":200}]}

Select the am4-dense-27b row, require ready is exactly True, status 200, context_length exactly integer131072 (not bool), parallel_slots exactly integer1 (not bool), physical_resource exactly am4:127.0.0.1:18090, and model basename Qwen3.8-27B-Q4_K_M.gguf. If absent, malformed, stale, stopped or wrong, return ready False, parallel_slots0, observed_at, reason, gpu_placed None. Never raise for malformed payloads. On success preserve model, context_length, physical_resource, observed_at, return ready True, parallel_slots1, gpu_placed None, reason explaining that HTTP readiness does not prove GPU placement. Ignore unrelated aliases: multiple aliases for the SAME resource never make parallel_slots2. Conflicting duplicate rows of the selected alias must fail closed. Native model/rate/GPU placement must not be fabricated.

Tests need valid ready, ready False, absent row, malformed payload, stale timestamp, future timestamp, wrong model, wrong context including bool, wrong slot count, and two aliases for one physical resource. Keep helper under70 lines and tests under90 lines. First useful artifact is the actual helper; write that before tests or commentary. No unified patch needed for this leaf. CAPACITY-REVIEW.md must distinguish test results from assumptions and say integration is Codex's responsibility.
