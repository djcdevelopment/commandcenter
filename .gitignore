__pycache__/
*.pyc
.venv/
.venv-*/
*.egg-info/
.pytest_cache/
node_modules/
.env
*.log
hearth/var/
.mcp.json
runs/hearth/
.codex-temp/

# Local meeting/interview prep notes — kept out of the repo
/*-prep-*.html

# Session export bundles (an export artifact, not repo content)
session-export-*/
session-export-*.zip

# b70tools adapter-enumeration spill (b70_snapshot writes these relative to cwd
# on every telemetry call, so every rotation load regenerates them)
runs/enumerate/
runs/run/

# Audio Lab: WAV artifacts are large (~120 MB) and regenerable via pacing_bench.py.
# The JSON receipts and LISTENING_SCORECARD.md ARE tracked -- they are the durable record.
tools/audio-lab/samples/
