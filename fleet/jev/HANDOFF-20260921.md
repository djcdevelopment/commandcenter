# First JEV loop: handoff and retrospective

Historical first-loop report. For the later count correction, HTTP 529 hold,
and restored current state, read [the latest handoff](HANDOFF-COUNTS-20260921.md).

Read [README](README.md) for implementation and recovery. The approved retry
produced real artifacts with assisted recovery; the candidate is **not accepted**
and the pilot is **not yet qualified for unattended operation**.

## Approved retry: the current result

Derek approved one clearer metadata-only request, keeping the original gates.
The clarification distinguished a pure formatting-function edit from building
the scheduler/hardware it describes. Concrete fit levels replaced generic
poor/partial/good/strong labels. No source or raw logs went to TypeSafe.

| Observation | First call | Approved retry |
| --- | --- | --- |
| JEV fit / confidence / ambiguity | 2.27 / 0.27 / 0.37 | 3.00 / 1.00 / 0.10 |
| API elapsed | 0.298 s | 0.323 s |
| Input tokens | 679 | 990 |
| Outcome | Held | One real worker dispatched |

Two calls cost an estimated **USD 0.000070098 total**. This is task/profile fit,
not a probability of patch correctness. No further JEV or model retry occurred.

At 06:13:22 UTC, JEV dispatched `cc-builder-2` through `omen-resident-hearth`:

- Receipt: `br-20260921-061321-d10b5edc`.
- Plan: `hearth-hermes-br-20260921-061321-d10b5edc-c5ea3cdd`.
- Worker model: actual HEARTH runner `qwen3-30b-a3b`, native 16384 context.
- Candidate commit: `428aaed3405f5e123f21e6f02055a0045de66f8c` on the isolated
  `ccfarm/<plan>/cc-builder-2/lap1` branch. One attempt, no timeout, file changed;
  completion observed at 06:15:17. Candidate branch push is not main promotion.
- [Unedited worker source](evidence/20260921-worker-candidate.json).
- [Unedited Hermes review](evidence/20260921-hermes-review.json), 47.135 seconds,
  one on-demand AM4 Dense run, no KV restore.
- [Independent executed finding](evidence/20260921-independent-review.json).

The worker's new count checks use `isinstance(value, int)` at candidate lines
59 and 63, accepting booleans. An actual call returned `active True; pending
review False`; the requirement was `unknown` for both. Hermes returned **PASS**
and incorrectly claimed that being subclasses of int means booleans are rejected.
Independent verdict: **NEEDS_WORK**. Syntax and the original four-line output
passed the actual checks; the failure is not inferred from a superficial diff.
The minimal correction is `type(value) is int` in those two count checks.
No correction has been attributed to the worker or silently promoted.

Two Codex integration defects needed assisted recovery:

1. The adapter initially looked for top-level `delegation`, but the real receipt
   stores it under `execution.delegation`. The actual build had been submitted.
   The existing receipt/queue was reconciled and the adapter/capture fixed;
   **no second build was submitted**.
2. The AM4 startup captured a transiently empty `/proc/<pid>/cmdline`, hashing
   `[]`, so later stop correctly refused the changed fingerprint. Matching start
   ticks plus complete baseline argv proved this was the process we had launched.
   Recovery repaired only that proven identity; the saved report was delivered
   without another Hermes call. Future starts bind to the launched argv and
   verify it after readiness. The fix was exercised during baseline restoration.

At 06:21:00 the report was saved locally and the review model was verified
unloaded: 88 MiB / 15 MiB GPU use and no compute process. At **06:23:37** final
restoration was verified: AM4 Dense healthy at genuine 128k/one slot, conductor
original sources restored, gateways 8710/8712 healthy, pilot 8713 absent, FX99
scheduler unit inactive. The held client still has its forensic `HOLD`; do not
clear it or enable the service without reviewing this run. The queue says
`awaiting_manual_acceptance`, not accepted. The original formatter and shared
main branches were not changed by candidate promotion.

The next useful slice is a corrected candidate plus a reviewer supplied with
actual execution evidence. Do not tune JEV fit thresholds to compensate for a
Python type-check bug, and do not treat Hermes's PASS as an acceptance gate.
Unattended release/status recovery needs one clean real lap before promotion.

## First lap history (superseded by the approved retry above)

## Outcome

The live TypeSafe integration authenticated using Derek's explicitly authorized
temporary credential. The key is private on FX99, directory `0700`, file `0600`;
the temporary protected Windows transfer file was removed. Rotation is still
expected within Derek's stated 24-hour window. No key belongs in this report.

JEV `jev-1.13.0` answered the actual ready task in **0.298 seconds**, using
**679 input / 95 output tokens**, estimated **USD 0.000028518**. It returned:

| Measurement | Observed | Admission policy |
| --- | --- | --- |
| Fit | 2.27 / 3 | >= 2.0 |
| Confidence | 0.27 | >= 0.60 |
| Task ambiguity | 0.37 | <= 0.25 |

The local operator validator passed all 19 checks, including schema/identity,
fresh capacity, context, deadline, cost and dispatch/receipt authority. JEV's
confidence and ambiguity gates then held the task as `needs_clarification`.
**No builder was dispatched. No locally authored candidate or Hermes review
was delivered.** The successful API call is not a substitute for that artifact.

AM4's genuine 131072-context, one-slot Dense baseline was captured privately,
then unloaded by its verified owner. `nvidia-smi` subsequently reported 88 MiB
on the 5070 and 15 MiB on the 4070 Ti, with no compute process. That verifies
the release, not a saved-KV restore or an end-to-end review cycle.

## End state at 06:07:01 UTC

The latest useful dispatch time elapsed without a retry decision. The first
lap ended after 7 minutes 35 seconds, not after consuming the 30-minute ceiling.
AM4's exact baseline was restored and native health returned `ok`; GPU memory
was 11378 MiB on the 5070 and 10873 MiB on the 4070 Ti. The two conductor files
were restored from verified backups while idle. Existing gateways 8710/8712
returned `ok`. Only the pilot-owned 8713 gateway and tunnel were stopped.
The FX99 scheduler service remains inactive. Credentials, source, private
baseline, held queue task, and evidence are retained for a deliberate restart.

The `status.json` file is the **last cycle observation**, not proof that a
daemon is currently running. Inspect the systemd unit/lock before calling it a
live scheduler. The richer status formatter is still the pending worker task.

## Evidence to resume from

- Overall receipt: `br-20260921-053442-f52caaab`.
- Portable, credential-free record: [first decision](evidence/20260921-first-decision.json).
- Queue ID: `jev-8bc92b76ef510e1e3e75c555`; still ready, no dispatch receipt.
- Candidate ID: `77ec76f36623838a1a3722df60bd10f7`.
- OMEN private evidence: `C:\Users\derek\.fleet-scheduler\first-live-decision.json`.
- Admission: `queue/<queue-id>/admission.json` under the same private root.
- FX99: `last-decision.json`, `api-budget.json`, `status.json` under
  `/home/derek/.local/state/fleet-scheduler`.
- The actual source-packed brief is in the local queue document. Do not send
  that whole document to TypeSafe: it contains original repository source.
- Deployment baseline hashes: `conductor-before.json` in the private OMEN root.

The API result is preserved separately before a retry could overwrite the
client's last-decision cache. Identical held inputs do not spend again.

## What the first edge means

The apparatus reached the real decision quickly, but the **decision contract
was not yet good enough to admit the real task**. The original fit rubric was
generic (poor / partial / good / strong), and the summary omitted the exact
behavioral contract supplied to the local worker. The score distribution put
82 percent of its mass on good/strong fit, yet the concentration-based
confidence was only 0.27. This is evidence about this prompt and provisional
policy, not proof that the worker cannot do the job or that JEV is unsuitable.

Cause is **not established** from one sample. Do not explain it away by blaming
the network, model, hardware, or confidence implementation without evidence.
Do not retroactively lower the threshold to relabel this held run successful.

Derek was asked whether to permit **one** clearer metadata-only retry with
concrete criteria and unchanged gates, or to stop qualification and deliver
the implementation plus this hold. Check his subsequent answer before making
another paid request or starting a builder. The original live window's latest
useful dispatch time was 06:04:26 UTC, with final delivery by 06:29:26; do not
silently reuse those expired deadlines on a later turn.

## Highest-value next lap

If the retry is approved, retain the exact worker task and hardware. Explain
the pure-function change and actual VM abilities in sanitized metadata, and
use a concrete fit rubric with distinguishable criteria. Preserve the first
decision. Cache identity must include the question/policy content so a changed
question is not silently treated as an unchanged old decision.

Keep the first artifact small: complete `fleet_status_format.py`, then an actual
Hermes final review exported from its session database. Review worker code
independently before integrating it. Distinguish worker authorship, Hermes
review, and Codex corrections. Do not turn this into a router/benchmark matrix
or new test scaffolding. One more refusal is useful data; record it and stop.

Only after that works should additional operator profiles be admitted. The
present selector ranks approved tasks on **one** qualified builder profile;
it cannot yet optimize alternate model/context/GPU placements. Unknown runtime
is reported as unknown, not filled with an invented estimate. DeepAgents,
multi-profile scheduling and saved-KV waking remain separate future slices.

## Accounting and authorship

Codex authored the adapter, integration, lifecycle helper and documentation.
The existing operator package/contracts/registries were reused from the
operator/program worktree unchanged. JEV authored only the observed decision
values in the first lap. In the approved retry, OMEN authored the linked worker
candidate and Hermes authored the linked review. Codex authored integration
fixes, ownership/delivery recovery and the independently executed finding.

Compile/import checks and isolated catalog generation passed. No new test files
were written. The live smoke was the requested task, not a synthetic benchmark.
The implementation remains experimental until the real dispatch/review path
produces its promised artifact and owned cleanup is observed afterward.
