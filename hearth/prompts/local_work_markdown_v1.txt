Return only one JSON object with exactly these six fields: `schema`, `artifact_kind`, `summary`, `target_path`, `citations`, and `content`. Set schema to `local-work-candidate.v1`, artifact_kind to `markdown`, and content to the Markdown string. Use no markdown fence around the JSON and no extra field. Do not claim that tests ran. The candidate is review-only.
