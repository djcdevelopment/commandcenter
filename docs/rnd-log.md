# R&D edge log — commandcenter

Lap-selection aid, not a backlog. One edge ends a lap. A clean sample is a result.

| Lap (UTC) | Probe | Edge found | Evidence | Re-run | Not sampled / suspected |
| --- | --- | --- | --- | --- | --- |
| 2026-09-18T01:29Z (cc-04e8b85c, lap 1) | Where does the control plane stop a prompt the AM4 dense rung (llama-server, 4096-token slot) cannot hold; which layer, loud or silent | **The validator does not see the pack.** A 5,336-token pack with the envelope claiming `max_context_tokens=4096` validated clean (it prices the envelope's claim against the rung's `context_bytes`, never the bytes it will send); llama-server refused it loudly through the facade — HTTP 400 `exceed_context_size_error` (n_prompt_tokens 5336, n_ctx 4096), no silent truncation; the attempt receipt kept only "HTTP Error 400: Bad Request" and dropped the body that says why | `runs/operator/run-rnd1-over6k-20260918T012916Z` (attempt_ef7e37ea…, `outcome.final` failed, 0.203 s); 400 body via direct curl to `192.168.12.233:8090/v1/chat/completions` | `bash <scratchpad>/rnd_lap1.sh over6k 4096 hearth/operator/proposal.py` from the operator-program worktree with `HEARTH_API_KEY` + `HEARTH_ROOT` set | a 3.5k–4k pack (does `max_tokens=1024` output push a near-full prompt over `n_ctx`?); the same probe through the door (pinned `am4-dense` should refuse at 14,336 bytes before dispatch — untested); drafter counting the pack's bytes itself; ingesting the 400 body into the receipt (suspected 3 lines in `execute.py`, deferred) |
| 2026-09-18T01:34Z (cc-04e8b85c, lap 2) | Derek's pivot: one NVIDIA card alone as a long-context READER (NVIDIA prefill beats the B70s); can the RTX 4070 Ti serve qwen2.5-14b at 16k through the AM4 facade + control plane, taking the 5k pack the 4k dense slot refused? | **Yes — clean sample, thesis holds.** `am4-read-4070ti` (14B Q4, 16k ctx, q8_0 KV, 10,161 MiB on the 4070 Ti alone): the 5,101-token pack validated, executed in **3.062 s** end-to-end, 5/5 checks, reconstructable; server prefill **2,849 tok/s** (5,096 tok in 1.79 s), decode 39 tok/s. Split-27B stopped (`dense.hold` set; `sudo -n` works over ssh); `am4-dense` now reports ready=False loudly. `execute.py` takes endpoint/bearer/model from the pool by rung name; scheduler probes every am4 facade rung | `runs/operator/run-rnd2-reader6k-20260918T013436Z` (attempt_31624be9…, artifact ee688d08…); `am4:~/work/am4-dual-nvidia-poc/logs/reader-4070ti.log` | `bash <scratchpad>/rnd_lap2.sh reader6k 16384 hearth/operator/proposal.py` (reader must be up on am4 :18085) | RTX 5070 as a second independent reader (next lap); a same-shape 5k pack on omen-arc for the B70 prefill number (G4's 34.9 s was a 10k pack — not comparable); the 16k ceiling; concurrent reads on both cards; a drafter rule sending `read_vs_reasoning=read` tasks to reader rungs (default still direct_hearth); the reader is a nohup, not a unit |
| 2026-09-18T01:52Z (cc-04e8b85c, lap 3, MemSplice `C:\work\memsplice` @ 730f4bc) | Run the MemSplice experiment for real: prefill on AM4's RTX 4070 Ti, ship the KV over the 1 GbE direct link with the MemSplice wire header, restore into the same model on one B70 (`qwen14b-vk1`), decode there; time every hop against the B70 prefilling itself. (The PoC's `LD_PRELOAD` capture cannot work on CUDA — device pointers, wrong symbol — so llama-server slot save/restore is the capture.) | **Restore REFUSED on the slot-state version**: `state_seq_load_file unknown (magic, version) 67677371/00000003` — AM4 `81aeaeb` writes v3, OMEN knee fork `e85caa8` reads v2. Everything before it ran with a real 989,019,916-byte KV (5,030 tok, 196,624 B/tok f16): AM4 prefill **1,728 ms (2,912 tok/s)**, save 261 ms, wire **8,405 ms at 112 MB/s**, sha equal both ends; B70 cold baseline **6,492 ms (775 tok/s)**, decode 20.6 tok/s. **Verdict for this pair: f16 KV over 1 GbE costs 1.67 ms/tok to ship vs 1.29 ms/tok B70 prefill — the wire alone loses**; break-even needs KV ≤ ~157 KB/tok (q8_0 marginal, q4_0 ≈ 1.5× win) or a 2.5/10 GbE link | `memsplice/results/{stage1.json, b70-cold-baseline.json, disaggregated-kv-20260918T015435Z.md}`; window `rot-memsplice-20260918-A` (closed `failed`, reason recorded); the version line in `hearth/var/swap-logs/qwen14b-vk1.log` | `cd C:\work\memsplice && python tests\benchmarks\bench_disaggregated_kv.py stage1` → door `rotation_window open` / `rotation_load qwen14b-vk1` / `restore_step.py` under `with-gateway-env.cmd` / `stage2` | stage 2 (decode from the shipped KV, `decode_identical`) — blocked on the version; same llama.cpp commit both ends (CUDA build of the knee fork on AM4, or rebase); q8_0/q4_0 KV to shrink the file; the 5070 as a second prefill card; a 10k+ prompt where AM4's prefill lead grows |
| 2026-09-18T01:47–01:49Z (cc-04e8b85c) — **can't answer why**, not a lap | Production `omen-arc` rate check before a rotation window (preflight G2) | Production decodes at **22–32 tok/s** (3 fresh samples: 30.7 / 22.9 / 31.5 over 32 / 32 / 692 tokens) against the 105.3 baseline; the same server instance (epoch ~23:20Z 09-17) did 90.2 on its first decode at 23:40Z and drifted 54 → 48 → 46 → 17 → 52 → 19 → 24 → 11 within the epoch; load report clean (49/49 layers on Vulkan0/1, 167 MiB host); a 692-token burst did not restore it; fx99 keep-alive timers had been stopped since 09-13 (nightshift morning step never ran) — restarted 01:46Z; no other intervention (INC-2026-08-30-A: watch, do not poke) | `hearth/var/arc-serve.log` `print_timing` rows; `hearth/var/arc-keepalive.jsonl` tail | `ssh fx99 sudo systemctl start arc-keepalive-deep.service` then `query_rung_state` | whether `ArcServeBoot` restores 105; thermal/clock; WDDM paging (ADR-0043) vs a real regression; whether the drift correlates with the G3/G4 runs' deep-context slots |
| 2026-09-18T02:12Z (cc-04e8b85c, lap 4a) | Derek's edge: the RTX 5070 (Blackwell, GDDR7 672 GB/s) is the newer card — is it the faster prefill seat? Second 14B reader on the 5070 (:18086), same flags as the Ti reader, same 5,030-token prompt, two rounds each, slot erased before every run | **No — the 4070 Ti wins prefill by 17%**: 5070 **2,493 / 2,500 tok/s**, 4070 Ti **2,920 / 2,929 tok/s** (2,017 vs 1,720 ms). Build carries `89-real;120a-real` (native Blackwell kernels present), so it is physics: prefill is compute-bound and the Ti has 60 SMs / ~40 TFLOPS vs the 5070's 48 SMs / ~31; GDDR7 bandwidth buys decode, not prefill. **Ti = prefill seat; 5070 = second reader.** Both readers resident (10.1 GB each) | `am4:~/work/am4-dual-nvidia-poc/logs/reader-5070-f16.log`, `reader-4070ti-f16.log` (print_timing rows) | `scp results/stage1-body.json am4:/tmp/memsplice-body.json; ssh am4 'for port in 18086 18085; do curl -s -X POST http://127.0.0.1:$port/slots/0?action=erase -d {}; curl -s http://127.0.0.1:$port/completion -d @/tmp/memsplice-body.json; done'` | 5070 decode rate (should beat the Ti on bandwidth — unmeasured); both cards prefilling two prompts concurrently (the prefill-farm shape); the 30B-A3B split with `--main-gpu` on the Ti instead of the 5070 (plan said 5070 — revise) |
| 2026-09-18T02:17Z (cc-04e8b85c, lap 4, MemSplice) | Same llama.cpp commit on both ends (`e85caa8`: OMEN's knee Vulkan binary; CUDA build of it on AM4, `-DCUDAToolkit_ROOT` + `-L/-rpath-link` on the toolchain lib dir was the missing link flag), then stage 2: restore the CUDA-made KV into the B70 and decode | **Cross-vendor KV hand-off is CORRECT**: restore 5,030 tokens / 989 MB in **309 ms**, replay `prompt_n=1 cache_n=5029`; B70 decode from the shipped cache **identical to its own cold decode for 64 tokens at T=0**. Speed: disaggregated **11,562 ms** (1,734 prefill + 1,114 save + 8,406 wire + 309 restore) vs B70 local prefill **6,542 ms** → **0.57×**, wire = 73% of it — the physics said so; the correctness proof is the result | `memsplice/results/disaggregated-kv-20260918T021803Z.{json,md}`, `results/restore-receipt.json`, `results/stage1.json`; window `rot-memsplice-20260918-B` (closed `passed`); memsplice README result section | `cd C:\work\memsplice && python tests\benchmarks\bench_disaggregated_kv.py stage1` → `rotation_window open` / `rotation_load qwen14b-vk1` / `restore_step.py` (with-gateway-env) / `stage2 --restore-receipt results\restore-receipt.json` | lap 5: 30B-A3B (4 KV heads) with q8_0 KV on both ends at 16k/32k — the projected win; q8_0 restore across vendors (untested: quantized cache types must match and the Vulkan q8 KV path is the yaml's own open note); the streaming detour on `llama_state_seq_save_file`; save_ms varied 261 → 1,114 ms between runs (disk cache) — the wire, not the save, is the cost |
| 2026-09-18T02:23Z (cc-04e8b85c, lap 5, MemSplice) | The use case with the odds: Qwen3-30B-A3B (4 KV heads), q8_0 KV on both ends, 16k and 30k prompts; prefill on AM4's two NVIDIA cards (layer split, CUDA e85caa8), wire, restore into ONE B70 (standalone Vulkan seat, same commit, production held down under `arc-maintenance.stop`), decode; vs the B70 prefilling itself | **WIN — 3.07× at 16k, 5.29× at 30k.** 16k: AM4 prefill 2370 ms (6674 tok/s) + save 228 + wire 7.0234 s (112.21 MB/s, 52,240 B/tok) + restore 427 ms = **10048 ms** vs B70 local **30836 ms** (513 tok/s). 30k: **19556 ms** vs **103508 ms** (288 tok/s — the knee: 3.5 ms/tok). Hot decode from the shipped q8_0 cache is coherent and gives the same answer but is NOT byte-identical (first-token phrasing differs) — CUDA vs Vulkan q8_0 rounding; f16 was identical in lap 4. Also: `ArcServeRestart` kills every `llama-server.exe`, and without the sentinel the watchdog re-boots production within minutes — the nightshift runbook step, relearned | `memsplice/results/moe-longctx-20260918T022840Z.{json,md}`; `hearth/var/swap-logs/memsplice-moe-8095.log`; `am4:~/work/am4-dual-nvidia-poc/logs/moe-dual-q8kv.log` | seats: AM4 `llama-server --model Qwen3-30B-A3B... -ts 1,1 -c 32768 -ctk q8_0 -ctv q8_0 --port 18090 --slot-save-path kvslots/` (e85caa8 build); OMEN `llama-server.exe -m ...Qwen3-30B-A3B... -c 32768 -np 1 -ctk q8_0 -ctv q8_0 --port 8095 --slot-save-path E:\work\battlemage\kv\` with `GGML_VK_VISIBLE_DEVICES=1`, sentinel set; then `python tests\benchmarks\bench_moe_longctx.py --tokens 16000 --tokens 30000` | streaming the KV during prefill (detour on `llama_state_seq_save_file`) — would cut the AM4-side 2.6–5.9 s; dual-B70 local prefill (production shape, `-ts 1,1`) as the fairer baseline — the 1,354 tok/s at 10k this epoch says the split helps but not 5×; whether q8_0 divergence grows with decode length; q4_0 KV (half again on the wire); the 5070 alone for the MoE (3B active might fit one 12 GB card at short ctx) |
| 2026-09-18T02:29Z (cc-04e8b85c) — follow-up to the can't-answer-why row | Does a fresh `ArcServeBoot` restore production's decode rate? | **Yes: 97.3 tok/s** (692 tokens in 7.11 s) right after the boot, vs 22–32 tok/s before it. The evening's degradation is within-epoch drift that a restart clears, not a regression; the cause of the drift is still unexplained | `hearth/var/arc-serve.log` (new epoch), the door result for job_7896f979 | `schtasks /Run /TN ArcServeBoot` then one 700-token pinned `omen-arc` call | what drifts (WDDM paging vs fragmentation vs thermal); how fast it drifts on a quiet box (the keep-alive deep timer is back on fx99, so the tail will show it) |
| 2026-09-18T02:35Z (cc-04e8b85c, lap 6, MemSplice) | The fair baseline: same lap-5 KV files, but the OMEN seat is the 30B-A3B across BOTH B70s (`-ts 1,1`, q8_0, 32k — the production shape); it restores + decodes, and prefills the same prompts itself | **Win survives: 1.806× at 16k, 2.928× at 30k.** Dual-B70 prefill 887 / 520 tok/s (single card was 513 / 288); restore 248 / 431 ms; decode at depth unchanged (19.0 / 10.8 tok/s). Disaggregated 9869 / 19571 ms vs local 17826 / 57310 ms; the wire is ~70% of the disaggregated side. Production booted back `at_rate` (110 tok/s) afterwards | `memsplice/results/moe-longctx-dualb70-*.{json,md}`; `hearth/var/swap-logs/memsplice-moe-dual-8095.log` | sentinel + `ArcServeRestart`; dual seat `llama-server.exe -m ...30B-A3B... -sm layer -ts 1,1 -c 32768 -np 1 -ctk q8_0 -ctv q8_0 --port 8095 --slot-save-path E:\work\battlemage\kv\`; `python tests\benchmarks\bench_moe_longctx.py --tokens 16000 --tokens 30000 --omen-only-from results\moe-longctx-20260918T022840Z.json --label dualb70`; then remove sentinel + `ArcServeBoot` | streaming detour; q4_0 KV; the AM4 prefill seat as a control-plane route; the production `-np 8` shape (16k slots) cannot even take the 30k prompt — the disaggregated path handles a prompt production refuses |
