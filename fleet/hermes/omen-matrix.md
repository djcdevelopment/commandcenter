Create ONE HEARTH build receipt and dispatch it once, then return actual receipt and plan IDs. No source browsing or polling. This is approved production of a useful operating guide, not a benchmark. Use these exact arguments:

create_build_request: repo C:/work/commandcenter-hermes-fx99; title "AM4 controller OMEN worker operating matrix"; backend omen-arc; task drafting; execute false; deliverables ["OPERATING-MATRIX.md"]; acceptance_criteria ["A concise source-backed operating matrix accurately distinguishes the AM4 controller from OMEN workers and states the capacity limits and access paths."]

Request/BUILD BRIEF: Your FIRST action must write OPERATING-MATRIX.md, under 550 words. Then finish; do not browse directories, run tests or contact services. Use the verified facts below, and cite the supplied file paths as source pointers (not claims that you personally read them). Cover placement, access, observations, feedback/learning, scheduling decisions, failure behavior and limitations. Do not invent capabilities or claim tests were run. You are the OMEN local model producing this report; integration is Codex's job.

Facts supplied by the operator at 2026-09-20 02:49 UTC: FX99 hosts the CPU-only Hermes controller and session storage. Its model and compression use AM4 http://192.168.12.233:8090/v1, am4-dense-27b, 131072 context, one physical slot on the 4070 Ti + 5070; the native server :18090 PID 1738436 has not been restarted. AM4 is reserved from NEW Hermes worker inference. Existing MechNet/conductor builders cc-builder-2 and cc-builder-3 perform file edits and CPU builds. Their new per-run preset omen-resident-hearth uses http://omen.mshome.net:8083/hermes-worker/mcp, worker-specific X-Hearth-Key, and HEARTH local_generate pinned to omen-arc. Upstream model secret stays on OMEN. The gateway proxies to OMEN's existing qwen3-30b-a3b; fresh native observations show 8 physical slots of 16384 tokens EACH, zero busy at observation. These are NOT eight 128k slots. No model or GPU serving configuration changed. Output-reserved token admission refuses overflow; no cloud or AM4 fallback; at most two named workers, no substitutions. Busy/unavailable workers wait only within their bounded job window. Manual promotion: model outputs are candidates, not automatic main merges. AM4 controller can remain responsive during OMEN work.

Source pointers: fleet/hermes/fx99_cli.py (controller route); fleet/hermes/remote/worker/runner_presets.py and agent_hearth.py (worker route); hearth/kernel/hermes_worker.py (native admission); hearth/kernel/governed_operator.py (policy); hearth/toolsurface/scheduler.py (advisory capacity scheduling); hearth/execution/service.py (accounted inference); fleet/hermes/remote/conductor/hermes_run_policy.py (manual promotion). Knowledge findings and accepted-artifact receipts inform future selections; they are evidence-derived routing feedback, not model-weight training. DeepAgents is not newly wired by this rollout; existing MechNet builders and HEARTH carry it. MemSplice and 256k Dense worker rotation remain explicit follow-ups, not prerequisites.

After create_build_request returns, execute_build_request with its receipt_id, mode delegate, backend omen-arc, builders ["cc-builder-2"], max_age_s 300, task_class drafting, promotion_policy manual, runner_preset omen-resident-hearth. Return IDs and stop. Never say dispatched unless the tool returned a plan ID.
