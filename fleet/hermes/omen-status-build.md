Create ONE build receipt and dispatch it once. No browsing or polling; return the actual receipt and plan IDs, then stop. This is real implementation for the new hermes-fleet fleet-status command.

create_build_request: repo C:/work/commandcenter-hermes-fx99; title "OMEN worker fleet status formatter"; backend omen-arc; task build; execute false; deliverables ["fleet_status_format.py"]; acceptance_criteria ["A pure dependency-free format_status(state) function accurately displays AM4 controller and OMEN worker capacity, fails closed on unready observations, and does not expose credentials or raw input."]

Request/BUILD BRIEF: FIRST action: write fleet_status_format.py. The complete task specification is supplied below; no browsing, no source reads, no network. After writing, optionally run ONE small inline Python smoke of your function, then finish. Maximum 75 lines. Do not write extra reports or tests. Codex will review and integrate your function. You are the OMEN local model authoring the formatter, not the integration.

Implement format_status(state: dict) -> str using only standard library, no I/O. Read only explicit fields from the dictionary; NEVER dump arbitrary dictionaries because future inputs may contain secrets. Input schema: {"controller":{"host":"fx99","backend":"am4","model":"am4-dense-27b","ready":true,"context_length":131072,"parallel_slots":1,"busy_slots":0},"worker":{"backend":"omen-arc","model":"qwen3-30b-a3b","ready":true,"context_length":16384,"parallel_slots":8,"busy_slots":0,"free_slots":8},"policy":{"max_builders":2,"promotion":"manual","fallback":"none"}}. Dictionaries/fields may be missing; ready is true ONLY for exact boolean True. Missing or malformed capacity values render "unknown", not zero. Capacity integers must be nonnegative and must not accept bool. If ready is not True, capacity must render "unknown" even if stale positive numbers remain in the input. Render 4 concise lines: placement/controller model and readiness; controller context per slot, total physical slots and busy slots; worker backend/model, readiness, context per slot, total/busy/free slots; fixed policy "At most 2 OMEN builders; manual review; no cloud or AM4 worker fallback." Fixed identities are allowed; do not claim 8 x 128k or a single-B70 allocation. Context length is per slot, never multiply it. No HTML, ANSI, shell strings or markdown needed. Escape newlines/control characters in any displayed model labels or preferably use the fixed approved model labels above. If state is not a dict, treat it as empty. Return complete text ending with newline. Do not assert that tests pass unless actually executed. This task needs no repository context.

execute_build_request: returned receipt_id; mode delegate; backend omen-arc; builders ["cc-builder-3"]; max_age_s 300; task_class build; promotion_policy manual; runner_preset omen-resident-hearth. Never say dispatched without the returned plan ID. Do not retry a failed dispatch.
