# Claim register — the authoritative lookup

**What this is.** One row per campaign claim that has been corrected, refuted, or bounded, with the
decisive receipt and the rule that caught it. It exists so an old headline cannot escape back into a
future ADR, plan, or article by being quoted from a document that was true when it was written.

**How to use it.** Before citing any throughput, ratio, crossover, or tax figure from this campaign,
look it up here. **If a claim appears in this register, the "corrected claim / bound" column is the
citable form — not the original, wherever the original still appears in prose.** Original receipts
and prose are preserved everywhere and annotated in place; nothing was rewritten.

Receipts: `E:\work\battlemage\ff-probes\ff-receipts.jsonl`, addressed by `probe` + `cell`; ETW captures under `E:\work\battlemage\ff-probes\etw-recorder\captures\`.
Rules: `docs/FACTORY-FRONTIER-CARDS.md` § *Campaign rules earned the hard way*.

---

## Corrected, refuted, or relocated

| # | Original claim | Status | Corrected claim / bound | Decisive receipt | Rule |
|---|---|---|---|---|---|
| 1 | `-ub 1024` causes a **~4× decode regression** at `-np 2` | **REFUTED** | Decode-neutral (**+0.4%**, inside drift). Prefill −1.3% @512, **+5.8% @2048, +12.3% @8192**. `-ub 1024` is **promoted**. | `UB-AB-WARM` | R1 R3 R6 R8 |
| 2 | FF6c: the `-ub` optimum moves with placement (crossover surface) | **SUPPORTED** | Reproduced server-side: the gain is prompt-length dependent and **negative at 512**. | `UB-AB-WARM` | R3 |
| 3 | Dual-split buys **+27–42% prefill**, costs **−5% decode** | **LOCATED, and it is model-specific** | *MoE surface:* sign change between **512 and 1024**; −1.8% @512, +37.9% @1024, ~+45% @1536–2048, **+72.2% @8192**; decode cost **~5–6%** (not a precise value). | `B3-TOPOLOGY-CROSSOVER` | R3 R4 R6 R7 R8 |
| 4 | Co-residency taxes **Flash** −42% (5.04 → 2.93) | **REFUTED + REASSIGNED** | Flash pays **−2.3% to −3.9%**, inside its own drift floor — *unresolvable from zero, but −42% excluded by >3× that floor*. The cost lands on the **incumbent**: **−15% to −28%** while sharing, **fully recovered** after. | `B4-FLASH-CORESIDENCY` | R1 R9 |
| 5 | Dense seats run **~6× slower per seat** than the MoE | **CORRECTED** | **4.5–5.0×** (4.51× / 4.65× dual, 4.76× / 5.03× single). The dense side reproduced exactly (23.52/23.62 vs 23.54); the **MoE side was a llama-bench number**. | `B5-DENSE-VS-MOE` | R8 |
| 6 | **121.6** tok/s "full-B70 dual-split" | **MISLABELED** | Its receipt reads `tensor_split "1.00"` — a **single-card llama-bench `tg128`** run. Server, solo: **116.35 single / 109.97 dual**. | `W-A-SOLO` | R8 (A4) |
| 7 | **Co-residency poisons the incumbent** (ADR-0041's trigger) | **TRIGGER SUPERSEDED** by `docs/adr#0043` | The loss is real; the trigger is **idle time**. >~60 s idle → a stable ~3.9× degraded state. Co-residency is **neither necessary nor sufficient** — running any experiment merely leaves the incumbent idle. | `W-B2-DELAYED-ONSET`, `W-B3-IDLE-LADDER` | R1 R6 |
| 8 | Sustained-rate decay that recovers after idle | **RETRACTED, then PARTLY VINDICATED** | Real, but only in the **post-idle** state. A *fresh* server holds 102–107 flat over 40 requests — which is why it looked refuted. Two machine states, not one contradiction. | `W-B3-IDLE-LADDER` | R1 R6 |
| 9 | Door overhead is large and non-constant (~91% of a call) | **RESOLVED — non-issue** | Door overhead is a **flat 175–264 ms independent of size** (35.6% of a 32-token call, 11.8% of a 512-token one). The apparent 91% was the rung's own first-request stall, invisible without a server-side join. | `C2-DOOR-ATTRIBUTION` | R3 R8 |
| 10 | `GGML_VK_VISIBLE_DEVICES=1,2` is **load-bearing** — never remove it | **REFUTED** (`docs/adr#0042`) | Removed, not corrected. Enumeration is nondeterministic per process launch; select by device **TYPE**. No index scheme is safe, `-dev`/`--device` included. | `FF-CENSUS` + `-lv 5` load report | R8 |
| 11 | LZ1-A's "favourable anomaly": 12.4–14.9 co-resident vs 10.6 solo | **EXPLAINED AWAY** | There was **no solo LZ1-A comparator** — 10.6 came from a different rig. Solo reads **14.40 / 14.71**, at or above every co-resident figure. Nothing to explain. | `W-A-SOLO` | R8 |
| 12 | The `-ub 1024` promotion is confirmed by the running server | **PROVENANCE-LIMITED** | `config_assertion: behavioral` — inferred from a rate separation (0.15% vs 12.5%), **not** read from a launch parameter. llama-server prints no `n_ubatch` at default verbosity. Strong corroboration; not observation. | `UB-AB-PROVENANCE` | R2 |
| 23 | The degraded state burns **+26–58% energy per token**, therefore **each token costs the GPU more work**, and a plain host-side stall is **disfavoured** | **PREMISE REFUTED — the energy figures are a SYMPTOM, not a mechanism** | The J/token numbers stand. The inference from them does not. It assumed *GPU not busy → GPU approaches idle power*; the directly observed clock/voltage state **disproves that premise** — a stalled GPU here stays pinned at **2800 MHz / 1.04–1.06 V** and burns near-load power while waiting. Measured directly: **GPU busy/token +5.5%** while **wall/token +97.4%**. Raised J/token is **prolonged wall residence at near-load power**, not extra GPU work. **Both derived candidates retired as primary explanations** — inefficient kernel/dispatch (busy/token would rise far more) and busy-wait spin (would raise the busy counter; it barely moves). | `LZ-STATEWATCH` phenotype (`lz-receipts.jsonl` row 168) | R1 R2 |
| 30 | A **board duty cycle** says how loaded the card is | **BOUNDED — the denominator is a transient, and every duty figure in this campaign inherits it** | Duty is the fraction of cell wall time a card's dJ/dt exceeds **0.9× its own frozen reference burst p50** (143.93 W bus 9 / 102.64 W bus 4). That reference is a **~92-second prefill burst at 2 clients** (90 s declared, 92 one-second intervals, 8,192-token prompts, `cache_prompt:false`, 22 requests; `ref-20260909T085437Z`, frozen `7d06fb0`). The workloads it stands in for run for **hours**: the longest real one measured on this box is a **3.59-hour** imagegen session, 850 images at a pool duty of 1.90 of 2 — **~140× the reference window** — which Derek reports reaching **90 °C within thirty minutes**. So a duty number answers *"was this card above 0.9× what a 92-second burst drew"*, **not** *"was this card working as hard as it does under a real long workload"*. ⚠ **It is not a ceiling:** a real serving cell has already peaked **181.2 W over the 159.92 W reference p50** (#28). ⚠ **Nor a thermal steady state:** **no sustained-load capture exists on this box** — the longest in the corpus is ~260 ticks and every burn-in file carries exactly one temperature sample per adapter, so a burst p50 cannot show throttling that arrives at minute thirty. **Retired by capturing a sustained reference** (a passive `b70tools --run` collector alongside a real hours-long imagegen run, which needs no fence of its own), never by rewording. The caveat now rides on every receipt and every reduced report. | `ref-20260909T085437Z`; `sat_cell_runner.DUTY_REFERENCE_CAVEAT` | R8 |
| 32 | The 2026-08-27 replica abort was **96 °C on the VRAM of `0000:04:00.0`** | **CONCLUSION CORRECT, CITATION CHAIN BROKEN** | The attribution is **right** — I confirmed it from the raw b70tools stream the watchdog wrote alongside (`04:00.0` VRAM 78 → 88 → 92 → 94 → **96** over five ticks, while that same card's **GPU tile read only 75**). ⚠ **But the abort receipt it is cited from cannot support it.** `watchdog-qwen27-replica-production-mtp-off-p512-c4-abort.json` carries a single scalar `max_temperature_c: 96` and **no per-adapter field at all** — no `hottest_adapter_bdf`, no `hottest_sensor`, no adapter array. Those fields were added to the watchdog **later the same morning**; `docs/adr#0038` and the prereg inherit the attribution from a **different** abort four hours later. ⚠ **The second card was NOT safe at the abort**: `09:00.0` VRAM was at **88 °C and still climbing ~2 °C per tick**. ⚠ **Ambient was never recorded**, and the cardboard HVAC duct was **OFF** — the operator note enabling it is stamped **2 h 26 m AFTER** this abort. Post-duct, 61 minutes of everything-at-once held **70 °C** on the same card. So the single largest term in every absolute temperature this lab has recorded is **unmeasured**, and duct state now goes on every receipt. | `temp-qwen27-replica-…-c4-*/events.jsonl` (raw stream), vs the abort receipt itself | R8 |

## Established here, not previously known

| # | Claim | Bound | Receipt |
|---|---|---|---|
| 13 | The rung **goes cold when idle** | 0/30/60 s idle: 106.5 flat. **120 s: 39.5** (replicated 39.71/39.54). Then a stable ~27.5 plateau. A **1-token ping every 20 s holds 104.83**. ⚠ Prevents the transition; does **not** reverse it. ⚠ **EXTENDED 2026-09-09 (#25):** *sustained load* does not reverse it either. | `W-B3-IDLE-LADDER` |
| 25 | **Warming does not recover a collapsed rung; one restart does** | After a **verified** 240 s idle (fx99 timers stopped, a single 299 s gap in `arc-keepalive.jsonl`): **41.97 / 28.00 / 28.19 tok/s ≈ 33%** of the 106.0 baseline — which **replicates #13's ladder** (its 120 s reading was 39.5, its plateau ~27.5). **24 sustained concurrent requests** then moved it only to **~48%**, unstable, each rep *lower* than the last (**72.0 / 50.06 / 30.44**, spread **81.8%**). **One restart: 106.45 / 106.25 / 106.19 = 100%**, spread **0.24%**, on the first warm iteration. ⚠ So `docs/adr#0043` Decision 2 ("warm, don't restart") is **narrowed to shallow decay (≥ ~64%)**; a collapse needs the restart, then the keep-alive to hold it. The restart remains the **discriminator** (`docs/adr#0044`): cleared by one ⇒ idle collapse; survives one ⇒ #20's class. ⚠ Every prior "unwarmed rep-1" figure in this corpus describes a rung the keep-alive never let go cold — this is the **first** verified idle. Cost: ~7 min of degraded production, no real traffic affected. | `p7-half2-cold-rung-20260909`, `remediation-20260909` |
| 26 | **`-np` was the unswept knob, and it was worth 58%** | Server-side sweep, first ever (`llama-bench` has no `-np`). Qwen3-30B-A3B Q4_K_M, 512-token prompts, dual layer-split, clients matched to slots — **regime, not a universal**. Jobs/hour: `-np 2` **2,128**, `-np 4` **2,769**, `-np 8` **3,358** (**×1.58**), `-np 16` **2,323** — so 16 **regresses ~32%** and 8 is the peak here. Per-request decode falls 66.8 → 44.3 → 25.4 while **aggregate** decode rises 133.6 → 177.2 → 203.2: the batching trade. ⚠ **KV does not scale with `-np`** on this build — **12,288 MiB at 2, 4 and 8 alike** — and compute buffers *fall*, 712 → 456 → 328 MiB/card. `-c` is the TOTAL and is divided, so per-slot context is `-c/np`. ⚠ Adding **clients** past the slot count buys occupancy, not throughput: 8→16 clients at `-np 8` moved jobs/hour 1.5% while duty went **0.168 → 0.632** and p50 power +30%. ⚠ **These jobs/hour figures are means over a BIMODAL floor** — see #31, the delayed-admission event, which grows with `-np` and is worth ~4% of a cell each time it fires. | `SAT-L1` cells `np{2,4,8,16}-p512-*`, prereg tag `prereg-np-sweep-lap1b-20260909` |
| 27 | **The MMV window is worth 15.8% under real serving load** | `GGML_VK_MMV_MAX_COLS` (ours, [PR 27652](https://github.com/ggml-org/llama.cpp/pull/27652)) at `-np 16`: widened-to-16 **2,206.9 / 2,439.5** jobs/h vs stock-8 **1,953.6 / 2,059.5**. Means **2,323.2 vs 2,006.6 = +15.8%**, and the two arms' **ranges do not overlap** (2,207–2,440 vs 1,954–2,060); per-request decode separates the same way (8.1/8.8 vs 7.1/7.5). Same binary, model, slots and prompts — only the env var changed. ⚠ **The knob clamps at 16**, so 16 is a ceiling, not a tuned value. ⚠ First measurement **under llama-server concurrency**; the knee campaign used `llama-bench` and frame pacing, and per-vendor serving data is the maintainer's named blocker. ⚠ Both arms sit on the bimodal floor of #31; the arms separate anyway because their ranges do not overlap. | `SAT-L1` cells `np16-p512-c16-r{1,2}` (widened) vs `r{3,4}` (stock) |
| 28 | **Layer-split is power-lopsided under `-sm layer -ts 1,1`** | At `-np 8`, 16 clients: `0000:09:00.0` **154.7 W p50 / 0.632 duty**, `0000:04:00.0` **81.5 W / 0.123** — one card doing roughly twice the work under a flag that divides the model evenly by ratio. Peak 181.2 W, **above** the 159.92 W frozen reference the duty metric is calibrated on. ⚠ **One cell**; recorded as an observation, not a law. ⚠ It is also the strongest argument for one-engine-per-card. ⚠⚠ **CORRECTED 2026-09-09, same day, by reading the backfill: my "never been measured" was WRONG.** The replica-per-card experiment produced **two clean cells before** the 96 °C abort, and **the partition won both** — 410.09 vs 396.75 jobs/h at 1 client (**+3.4%**) and 819.48 vs 636.06 at 2 clients (**+28.8%**), with better p95 in both, and replica decode held **23.05 / 23.10** flat where dual-split **fell 22.97 → 18.70**. ⚠ But the foundation is thin and must be quoted with it: **n = 3 and 6 requests**, ~26 s per cell, Qwen3.8-27B **dense** at `-c 65536 -np 1` per server, MTP off — not the 30B MoE at `-np 8`. ⚠ **The c4 row (736.53) is NOT citable**: it is the aborted cell, 10 valid requests of 12. ⚠ And the dual arm ran its full matrix to 656.5 at 24 clients while the replica stops at 2, so **the partition's curve past 2 clients is unknown**. What the quarantine stopped was the **rest of the matrix**, not the premise. | `SAT-L1` cell `np8-p512-c16-r1`; `corpus/backfills/qwen38-campaign-full-20260827.bench-row.v1.jsonl` (48 replica rows) |
| 29 | **q8_0 KV is a ceiling lever worth ~3 GB/card, and it costs 4.7% of decode** | Two halves that lived in two documents and were never priced together. **The benefit:** production runs **f16** KV — its own load report reads `K (f16) 6144.00 MiB, V (f16) 6144.00 MiB` — so `-ctk q8_0 -ctv q8_0` roughly halves 12,288 MiB and frees **~3 GB per card**, which is what would fund a deeper `-c`, more slots, or a co-resident side model. Derek's vllama has shipped q8_0 on this rig since June. **The cost, measured (FF4, 2026-08-29, 30B-A3B Q4_K_M, single B70, `-ub 512`, depth 8192, `-r 3` × 2):** decode **33.23 → 31.68 = −4.7%**, reproducible to 0.4%, while prefill is **flat** (461.4 vs 461.1–468.5, inside the 1.6% within-type spread). ⚠ **So KV quant is a ceiling lever, never a speed lever** — adopting it for throughput would be a mistake. ⚠ **Non-monotonic in bit-width: q4_0 decode BEATS q8_0 by +1.9%**, reproducibly, so if the ceiling is the goal then q4_0 is the better arm and q8_0 is the worse of the two. ⚠ The **ceiling half is still unmeasured here** — 27593 reports q4_0 fitting 131,072 context on one 32 GB B70 (f16 49,152, q8_0 65,536); our 131,072 quarantine was a *dual-split thermal* limit, so single-card + quantized KV has never been run. **Not taken:** a tenancy call on the rung every `:8082` consumer uses, plus an unmeasured quality question. Recorded at the point of use in `fleet/arcserve/llama-swap/omen.yaml`. | `FF4-kv`; production `hearth/var/arc-serve.log`; `docs/prereg/SATURATION-SURFACE-LAP1.md` |
| 14 | **Two** distinct post-idle costs | A **~11.5 s first-request stall**, size-independent and **often invisible to `print_timing`** (server reported `prompt_ms 47.1` while `launch_slot_`→`release` spanned 11.54 s); and the decode collapse. | `C2-DOOR-ATTRIBUTION`, `ARC-KEEPALIVE` |
| 15 | Contention is **local to the shared card and cheap** | A resident-but-idle neighbour costs the incumbent **nothing**; an actively inferring one on the same B70 costs **8%**, only while it infers; CPU-only and iGPU co-tenants cost nothing. | `W-B-POISON` |
| 16 | **Dense models are topology-insensitive on decode; the MoE is not** | Dense dual→single: **+0.4% / −2.2%**. MoE: **+5.9%**. So the dual-split decode cost is an **MoE property**. | `B5-DENSE-VS-MOE` |
| 17 | The prefill crossover is **model-specific** | MoE: between 512 and 1024. Dense Qwen2.5-32B: dual already wins at 512 (**+11.6%**), so its crossover is *below* the sampled range. | `B5-DENSE-VS-MOE` |
| 18 | The solo noise floor is **venue-dependent** | 0.01% decode spread on dual-split B70; **10.5%** decode and **50.9%** prefill on experts→CPU. A ±10% effect is unmeasurable on host-memory venues at 2 reps. | `W-A-SOLO` |
| 19 | **First eval costs ~12×, per batch shape** | Flash `prefill@512`: 4.80 tok/s cold (105.8 s) vs 57.28 warm — *after* the 22-token shape had already warmed the process. | `W-A-SOLO` |
| 24 | **Throughput loss in the degraded state is NON-BUSY WALL TIME, not extra GPU work** | *Channel B* (32-token deep probe, 11 healthy vs 2 degraded, one capture, one epoch, no restart): GPU busy/token **8.811 → 9.297 ms (+5.5%)** while wall/token **9.272 → 18.298 ms (+97.4%)**; host gap/token **0.461 → 9.001 ms**; GPU busy fraction **95.0% → 50.8%**. *Channel A* (1-token ping): of **+9.90 ms** extra prefill latency, **0.87 ms (9%) is GPU busy, 9.03 ms (91%) host-side**; threshold-free form — regressing busy-ms on prompt-ms over **all 117 clean pings** gives slope **0.090**. Instrument: `gpu.activity.global_counter`, IGCL, `DirectlyObserved`, **nanoseconds of GPU busy time**, B70 idle floor **0.008%**. Also promotes *one ping = one forward pass* from inference to measurement: **8.814 ms** (1 token) vs **8.811 ms/token** (32 tokens), **0.04%**. ⚠ **n=2 degraded — do NOT read the +5.5% as a secondary mechanism**; it is unresolved small-effect-or-noise and the localisation does not depend on it. ⚠ Per-adapter aggregate: cannot name an engine, cannot separate *host did not submit* from *host submitted late*. Localised **outside GPU execution and no further**. | `LZ-STATEWATCH` phenotype (`lz-receipts.jsonl` row 168) |

---

## Open — recorded, not resolved

| # | Observation | Bound | Receipt |
|---|---|---|---|
| 20 | **INC-2026-08-30-A** — a restart-surviving ~61% state | Appeared ~00:28 on 2026-08-30, stable across `n_predict` 8→400 and spaced checks, **survived a restart** (unlike ADR-0043's idle-cold state), cleared on its own by ~01:05. Placement, thermal, spill, co-tenancy and generation length all excluded by direct measurement. **Unattributed.** ⚠ **SIX episodes in 12.4 h** (8 of 145 deep-probe samples, 5.5% of sampled time), then **6.0 h clean**. The keep-alive ran throughout, so **prolonged idleness is not necessary** — the pre-committed negative result. ⚠ Most episodes are **≤5 min**, not the "stable state" the first one suggested, and depth ranges **69.22 → 1.24 tok/s**, so the earlier clustering claim is **WITHDRAWN**. Episode table in `docs/adr#0044`. ⚠ **MECHANISM-LEVEL LOCALISATION (see #24):** during the observed degraded states, throughput falls **primarily because the GPU spends substantially more wall-clock time not busy between approximately normal units of GPU execution work.** What remains is a *family*, not a mechanism: host scheduling, driver submission latency, synchronisation/waits, server slot/scheduler behaviour, or another delay between successive submissions. ⚠ **ETW ring captures** (`cap-20260830-190358-arc`: 70.84 → 106.88 tok/s; `cap-20260901-152356-arc`: 84.7 → 106.25 tok/s, 19.3 GB ETLs) confirm bounded ~5 min dwell and spontaneous recovery with compute queue depth telemetry (queues `0xFFFFBB041C43A150`, `0xFFFFBB03C9FACB40`). | `ARC-KEEPALIVE` deep probe, `FF-RATECHECK`, `cap-20260830-190358-arc`, `cap-20260901-152356-arc` |
| 31 | **The delayed-admission event — the largest single loss term in this campaign, with no named cause** | llama-server sometimes admits the second concurrent request of a round hundreds of milliseconds after the first, against a **0.2 ms** norm. **It is server-side:** client `started_at` skew is 0–12.5 ms. **⚠ It grows with `-np`, in both size and frequency** — ~222 ms at `-np 2` (222.0 / 222.4 / 250.7, so **not** quantized), **434–589 ms at `-np 4`**, **674–943 ms at `-np 8`** where *all three* rounds were delayed; and at `-np 8` the count **doubled 3 → 6** when clients went 8 → 16. It is not disappearing as the batch widens. **Cost:** one delayed round costs a whole cell **~4%** (4 of 5 block-2 repeats carried one at 2,097–2,125 jobs/h against clean r8's **2,203.7**). ⚠ **This makes the floor bimodal, not Gaussian, so a mean misrepresents it** — p50 spread 2.26% and decode 2.14% but **p95 spread 10.45%**, all tail. The honest statistic is how often a round is delayed; the reducer counts occurrences rather than averaging them. **Mechanism — hypothesis with strong support, persistence inferred, NOT proven:** prefill runs in three separable bands (**alone 1,982–1,995**, **beside a decode 1,840–1,890**, **batched 1,460–1,476** for ~2,940 aggregate), and a delayed admission knocks a pair out of phase permanently because nothing re-synchronises them. So the expense is the phase change, not the delay. Batched-prefill fraction — **not** client count — is the variable, and it is worth **10.1%**. ⚠ **Excluded by direct measurement:** the fx99 keep-alive, and my own hypothesis that `ff_cell`'s warm-discard probe held a slot (refuted at source — the lone warm request released 24–32 ms *before* the pair launched). ⚠ **The log route is fragile:** `llama-server` truncates `hearth/var/arc-serve.log` on every start (78 MB → 569 KB at one restart), so it only ever sees the current epoch; the durable route is per-round **TTFT asymmetry** in the harness's own rows. If it holds, keeping arrivals in phase is a submission-side lever worth ~5–10% — Lap 2's territory. | `SAT-L1` block 2 (`launch_skew`), `np{2,4,8}-p512-*`; `docs/prereg/SATURATION-SURFACE-LAP1.md` |
| 21 | The keep-alive's *"holds the rate"* claim | Validated over **~6 minutes**. An epoch ran ~35 min with pings and degraded anyway. Narrowed to the tested horizon; not evidence the keep-alive failed. | ADR-0043 |
| 22 | The **106.00 baseline** is a reference, not capacity | **Four** stable levels in one night (~106, ~97–99, ~65, ~27.5), with transitions in **both** directions and no intervention. Resolved into `docs/adr#0044`: health = **baseline epoch + observed rate + acceptance envelope**; the baseline is preserved, never silently redefined. ⚠ **Epoch-scoped ≠ epoch-homogeneous** — multiple regimes occurred inside one epoch, so a shared epoch does not make two readings comparable. Levels are **observed regimes, not established mechanisms**. | `FF-RATECHECK`, `docs/adr#0044` |

⚠ **R3 caught its own author on #20.** Diagnosing it, a single-arm revert of `-ub 1024` read
65 → 97 and nearly became "my promotion caused a 33% regression". The interleaved re-test found both
configs at ~97–98: a **state change read as a config effect** — the identical error the original B1
claim made, on the same flag, one day after the rule against it was written.

---

## There is no machine-level topology law

⚠ **Do not describe this box as having "a dual-split decode tax" or "the prefill crossover".**
Neither exists as a property of the machine. B3 established the surface for **one MoE**; B5 showed
dense models occupy a **materially different** one — insensitive on decode, and with the prefill
crossover in a different place.

The correct object is a **model × topology × workload surface**. Any claim of the form "dual-split
costs X" or "the crossover is at N tokens" is incomplete unless it names the model and the prompt
length. This is a more useful architectural result than the original headline it replaced, and it
is the form future placement policy should take.

---

## Non-identifiable from historical evidence — bounded, not repaired

Epoch **E2** (2026-08-28 08:32 → 2026-08-29 04:40) holds the entire LZ campaign window and all
pre-fix FF work — **288 of 317 receipts** — and is anchored **single-card**. Those receipts carry
`PLACEMENT_CONTEXT_UNKNOWN` / `PLACEMENT_CONTEXT_INVALID` and `INCUMBENT_HEALTH_UNKNOWN`.

**The policy is to bound, not to repair.**

1. **The labels stay.** They are the finding, not a defect to be cleaned up.
2. **Derive only conclusions that hold across plausible E2 states** — single- or dual-card,
   incumbent healthy or cold. A conclusion robust to all of them is usable as-is.
3. **A conclusion that flips depending on the state is `NON-IDENTIFIABLE FROM HISTORICAL
   EVIDENCE`.** It is marked so and left there. It is *not* an open measurement request.
4. ⚠ **A modern re-measurement is not a historical correction.** Re-running a cell today produces a
   fact about today's machine. It may supersede the old claim going forward, but it does **not**
   tell us what the August 28 machine did, and labelling it as though it did would manufacture
   provenance. Where both exist, they are two rows, not one corrected row.

Reconstruction tooling: `campaign/ff-probes/ff_provenance.py` (byte-stable, `--check` exits 1 on
drift). Its output is a **classification**, never a substitute measurement.

---

## Where the dominant uncertainty now sits

Not in the numerator. The five suspect measurements are closed and the instrument problems that
produced them are now gated (R8).

**It sits in FF1** — the work-slice harness, still unbuilt. Every work-per-machine-hour claim
divides by it, so orientation tax, continuity dividend, and prefill amortization all inherit an
uncertain denominator. With numerator quality substantially improved by this campaign, **FF1 is now
the dominant epistemic weakness in every higher-level efficiency claim**, and it is the priority.

**Extended 2026-09-09 — a second denominator, and one unexplained term.** The saturation surface
added two weaknesses of the same shape, and neither is in the numerator either:

- **#30 — the duty denominator is a transient.** Every duty figure divides by a ~92-second prefill
  burst standing in for workloads that run hours. Same failure mode as FF1: a well-measured
  numerator over a denominator nobody captured. Retired by capturing a sustained reference, and
  that capture is cheap — a passive collector alongside a real run, needing no fence of its own.
- **#31 — the delayed-admission event is the largest single loss term with no named cause.** It
  grows with `-np` in both size and frequency, it makes the throughput floor bimodal so means
  misrepresent it, and the mechanism is a hypothesis with the persistence step still inferred. Every
  jobs/hour figure this campaign reports is a mean over that floor.
